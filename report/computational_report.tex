\documentclass[11pt]{article}

\usepackage[margin=2.3cm]{geometry}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{array}
\usepackage{graphicx}
\usepackage{adjustbox}
\usepackage{xcolor}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}
\usepackage[capitalise]{cleveref}
\usepackage{url}
\usepackage{float}
\usepackage{enumitem}
\setlist{nosep,leftmargin=1.4em}

\definecolor{darkgreen}{rgb}{0.0, 0.5, 0.0}

% One shared axis style; legend outside the axis so it never covers a curve.
\pgfplotsset{pcfplot/.style={
  width=0.97\linewidth, height=5.1cm,
  tick label style={font=\scriptsize}, label style={font=\small},
  legend style={font=\scriptsize, at={(0.5,1.03)}, anchor=south,
                legend columns=3, draw=none, fill=none},
  grid=major, grid style={gray!25}}}

% One colour / marker / dash per algorithm, identical in every plot of this
% report and of the manuscript.
\pgfplotsset{
  pac/.style   ={color=red!80,          mark=square*,   thick},
  dpac/.style  ={color=red!80,          mark=diamond*,  thick, dashed},
  hpac/.style  ={color=darkgreen,       mark=*,         thick},
  dhpac/.style ={color=darkgreen,       mark=pentagon*, thick, dashed},
  rac/.style   ={color=blue!70,         mark=triangle*, thick},
  hipac/.style ={color=orange!90!black, mark=triangle*, thick},
  hopac/.style ={color=violet,          mark=square*,   thick},
  bppf/.style  ={color=brown,           mark=square*,   thick},
  aux/.style   ={color=black!55,        mark=o,         thick},
  aux2/.style  ={color=black!35,        mark=x,         thick, dashed}}

% Curves that are NOT algorithms (structural quantities of the instances, heap
% policies, densities) never borrow an algorithm's colour: they are drawn in
% greyscale, told apart by marker and dash pattern. q1--q3 for quantities
% plotted together, d1--d4 for the four densities (a light-to-dark ramp).
\pgfplotsset{
  q1/.style ={color=black,     mark=square*,   thick},
  q2/.style ={color=black!45,  mark=o,         thick, dashed},
  q3/.style ={color=black!70,  mark=triangle*, thick, dotted, line width=1pt},
  d1/.style ={color=black!55, mark=square*,   mark repeat=6, mark phase=1,
              thick, dashed},
  d2/.style ={color=black!70, mark=o,          mark repeat=6, mark phase=3,
              thick, dash dot},
  d3/.style ={color=black!85, mark=triangle*,  mark repeat=6, mark phase=5,
              thick, densely dotted},
  d4/.style ={color=black,    mark=*,          mark repeat=6, mark phase=2,
              thick}}

\pgfplotsset{famaxis/.style={xmin=0.5, xmax=6.5,
  xtick={1,2,3,4,5,6},
  xticklabels={ind-pos, ind-sgn, corr, anti, near, exact},
  xticklabel style={rotate=35, anchor=east, font=\tiny}, pcfplot}}
\pgfplotsset{rhoaxis/.style={xlabel={$\varrho$},
  xtick={0.3,0.6,0.9,1.0}, xticklabels={0.3,0.6,0.9,1.0},
  xmin=0.25, xmax=1.05, pcfplot}}
\pgfplotsset{sizeaxis/.style={xlabel={$n$}, pcfplot}}

\newcommand{\tab}[1]{\input{../results/tables/#1.tex}}
\newcommand{\dat}[1]{../results/tables/rep_plot_#1.dat}
\newcommand{\dfile}[1]{../results/tables/#1.dat}

% Every result is presented the same way: the numbers on the left, the same
% data plotted on the right, comment below.
\newcommand{\tabfig}[4]{%
  \begin{figure}[htbp]
  \centering
  \begin{minipage}[c]{0.46\textwidth}\centering\scriptsize
  \setlength{\tabcolsep}{3pt}#3\end{minipage}%
  \hfill
  \begin{minipage}[c]{0.50\textwidth}\centering#4\end{minipage}
  \caption{#2}\label{#1}
  \end{figure}}

% Same convention for the tables too wide for half a text width: the numbers on
% top at full width, the same data plotted below them, one caption for both.
\newcommand{\tabfigwide}[4]{%
  \begin{figure}[htbp]
  \centering
  {\scriptsize\setlength{\tabcolsep}{3pt}#3}
  \\[3mm]
  #4
  \caption{#2}\label{#1}
  \end{figure}}

\title{\vspace{-1.4cm}Parametric Maximum Closure on Directed Forests\\
\large Computational Report -- data release \texttt{v0.3.0}}
\author{Valerio Dose \and Fabio Furini \and Marco Locatelli}
\date{1 September 2026}

\begin{document}
\maketitle

\noindent
This report accompanies the manuscript \emph{``On parametric Maximum Closure
Problems over precedence forests''}. The manuscript states the algorithms,
their analysis, and the few experiments needed to support the theory. This
report is the complement: the full experimental record, and specifically the
material the manuscript does not carry --
\begin{itemize}
  \item absolute running times of \emph{every} algorithm at \emph{every} size
  of every campaign, not a selection;
  \item breakdowns by \emph{arc density} and by \emph{coefficient family},
  which is where the interesting structure turns out to be
  (\cref{sec:ccamp});
  \item what the instance generators actually produced -- arcs, components,
  number of closure layers -- so that a reader can tell what each parameter
  buys (\cref{sec:instances});
  \item the dispersion of the raw measurements, so that any reported ratio can
  be compared against the noise it rests on (\cref{sec:dispersion});
  \item the implementation analysis behind the $\mathcal O(n)$ space bound
  (\cref{sec:heap}), and the exact protocol, including what ``peak RSS'' means
  and why memory is only quoted from single-algorithm runs.
\end{itemize}
Every table and plot is generated from the raw benchmark data by
\texttt{tools/build\_report.sh}; no number is hand-typed.

\paragraph{How to read the figures.} Every result in this report is presented
in the same shape: a table of numbers on the left, a plot of \emph{the same
numbers} on the right, and one caption below both. Every caption follows the
same three-part order:
\begin{enumerate}
  \item \textbf{what is reported} -- the quantity and the statistic, spelled
  out (for instance ``median CPU time in milliseconds, the median over
  instances of the per-instance median over repetitions'');
  \item \textbf{on which instances} -- the instance class, the sizes, and how
  many instances each reported value aggregates;
  \item \textbf{what we observe} -- the reading of the figure, and where
  possible the mechanism behind it.
\end{enumerate}
Unless a caption says otherwise, every time is CPU time in milliseconds, every
``median'' is taken over instances of the per-instance median over repetitions,
and every ratio is a \emph{paired} per-instance ratio (\cref{sec:defs}).

\tableofcontents
\vspace{4mm}

\section{Summary}

\begin{itemize}
  \item \textbf{Sweep.} One frozen commit, one machine, 2026-08-31/09-01:
  16\,200 instances, 292\,080 timed runs, seven algorithms plus the external
  \texttt{BPPF} baseline. No run hit the 300\,s timeout or the 8\,GiB memory
  ceiling, so nothing is censored.
  \item \textbf{Correctness.} Zero cross-algorithm disagreements
  (\cref{sec:validation}), on top of an exhaustive enumeration oracle on small
  instances and sanitizer builds.
  \item \textbf{Random forests, paths, binary trees.} \texttt{HPaC} is the
  fastest of our algorithms: $2$--$13\times$ \texttt{RaC} depending on size and
  density, and faster than the direct scan \texttt{PaC} from $n\approx400$ on.
  \item \textbf{Stars.} The ordering inverts: \texttt{RaC} is
  $\approx250\times$ \texttt{HPaC} at $n=20\,000$. Any heap-based schedule
  pays $\Theta(n)$ maintenance per event on a hub; \texttt{PaC}, which
  maintains no candidate structure, resists an order of magnitude better than
  \texttt{HPaC}.
  \item \textbf{Single orientations.} \texttt{HIPaC}/\texttt{HOPaC} take
  $0.52$--$0.78$ of \texttt{HPaC}'s time, stable up to $n=10^5$.
  \item \textbf{Against pseudoflow.} \texttt{HPaC} is faster than
  \texttt{BPPF} by a median factor $3.3$ on forests with $n\le1\,000$, even
  with the breakpoints handed to \texttt{BPPF} for free; \texttt{BPPF} misses
  the exact layer sequence on 16 of 2\,400 instances, for precision reasons
  (\cref{sec:bppf}).
  \item \textbf{Two facts that organize everything else.} The \emph{topology}
  decides which algorithm wins -- density moves the margin by an order of
  magnitude and the star class inverts it outright -- while the
  \emph{coefficient family} decides only how much parametric structure exists,
  moving absolute times but never the ranking. And the data structure inside
  one algorithm can matter as much as the choice of algorithm: the two heap
  policies of \cref{sec:heap} differ by $4\times$ in time and two orders of
  magnitude in memory, at identical output and identical asymptotic bounds.
\end{itemize}

\section{The algorithms, in one page}\label{sec:algos}

All of them solve the same problem and return the same object: given a
directed forest with integer profits $p_i$ and strictly positive integer
weights $w_i$, the ordered sequence of closure layers of
$u(\lambda)=\max\{P(S)-\lambda W(S): S \text{ closed}\}$, with the exact
rational threshold of each layer. All of ours use exact integer arithmetic:
64-bit coefficients, 128-bit cross-multiplication for every ratio comparison,
no floating point in any decision path.

\begin{table}[htbp]
\centering
\caption{The algorithms compared in this report: the class of forests each one
applies to, its worst-case time and space bounds, and how it selects the next
candidate. Bounds are those proved in the manuscript, except \texttt{BPPF}'s,
which is the published bound of the parametric pseudoflow method on a graph
with $n$ vertices and $m$ arcs. A forest has $m\le n-1$, i.e.\
$m=\mathcal O(n)$, so on \emph{our} inputs \texttt{BPPF}'s bounds specialize to
$\mathcal O(n^2\log n)$ time and $\mathcal O(n)$ space: its time bound is worse
than that of every algorithm above it, which is the theoretical reason a
forest-specific method is worth having.}
\label{tab:algos}
\small
\begin{tabular}{@{}lllll@{}}
\toprule
& Applies to & Time & Space & Selection of the best candidate \\
\midrule
\texttt{PaC}   & any forest    & $\mathcal O(n^2)$      & $\mathcal O(n)$      & linear scan \\
\texttt{DPaC}  & any forest    & $\mathcal O(n^2)$      & $\mathcal O(n)$      & linear scan, dual order \\
\texttt{HPaC}  & any forest    & $\mathcal O(n^2\log n)$& $\mathcal O(n)$      & two candidate heaps \\
\texttt{DHPaC} & any forest    & $\mathcal O(n^2\log n)$& $\mathcal O(n)$      & two heaps, dual order \\
\texttt{HIPaC} & in-forests    & $\mathcal O(n\log n)$  & $\mathcal O(n)$      & one heap over the vertices \\
\texttt{HOPaC} & out-forests   & $\mathcal O(n\log n)$  & $\mathcal O(n)$      & one heap, dual order \\
\texttt{RaC}   & any tree      & $\mathcal O(n\log n)$  & $\mathcal O(n\log n)$& none: top-tree contraction \\
\midrule
\texttt{BPPF}  & general graphs& $\mathcal O(mn\log n)$ & $\mathcal O(m)$      & external, parametric pseudoflow \\
               & \emph{on a forest} & $\mathcal O(n^2\log n)$ & $\mathcal O(n)$ & \emph{since $m\le n-1$} \\
\bottomrule
\end{tabular}
\end{table}

\noindent
What each one does, and what the experiments below add:

\begin{itemize}
  \item \textbf{\texttt{PaC}} peels the current best final vertices or
  contracts the best arc, finding the best candidate by a linear scan at every
  iteration. Slowest on random forests beyond $n\approx400$, but the most
  robust of the three on stars, precisely because it maintains nothing.
  \item \textbf{\texttt{DPaC}} is its dual: it builds the sequence from the
  lowest ratio upwards. It tracks \texttt{PaC} everywhere we measured.
  \item \textbf{\texttt{HPaC}} performs the same two moves, but keeps the
  candidates in two heaps, so only those touched by the last move are
  refreshed. Fastest of ours on random forests, paths and binary trees;
  penalized on a high-degree hub, where a single move invalidates a constant
  fraction of the candidates.
  \item \textbf{\texttt{DHPaC}} is its dual, within $25\%$ of it everywhere.
  \item \textbf{\texttt{HIPaC}} exploits out-degree $\le1$: the minimal
  preceding set of an arc is then a singleton, so one heap value per vertex
  suffices and no closure sums are propagated. Takes $0.52$--$0.75$ of
  \texttt{HPaC}'s time. \textbf{\texttt{HOPaC}} is the symmetric case
  (in-degree $\le1$): $0.57$--$0.78$.
  \item \textbf{\texttt{RaC}} contracts the tree in $\mathcal O(\log n)$
  rake-and-compress rounds, computing cluster functions, then recovers the
  thresholds top-down. It is the only algorithm whose cost does not depend on
  how often a single vertex is touched -- hence the winner on stars, by up to
  $250\times$.
  \item \textbf{\texttt{BPPF}} is the external baseline: it solves a strictly
  more general problem, evaluates minimum cuts at parameter values supplied by
  the caller, and uses fixed-point arithmetic -- hence the precision caveat of
  \cref{sec:bppf}. Its $\mathcal O(mn\log n)$ bound is stated for a general
  precedence graph; a forest has $m\le n-1$, so on the instances of this report
  it reads $\mathcal O(n^2\log n)$: a factor $n$ above the
  $\mathcal O(n\log n)$ of \texttt{RaC} and of the single-orientation variants,
  and a factor $\log n$ above the $\mathcal O(n^2)$ of \texttt{PaC}. The
  measured gap of \cref{sec:bppf} should be read against that: it is a factor
  of a few, far smaller than the gap between the bounds.
\end{itemize}

\noindent
Two remarks that matter for reading the results. First, \texttt{PaC} and
\texttt{HPaC} are the \emph{same algorithm} under two schedules -- the
difference measured in \cref{sec:bcamp} is entirely the cost of finding the
maximum-ratio candidate. Second, \texttt{RaC} pays a structural overhead per
component (cluster bookkeeping) that the others do not, which is why density
matters so much in \cref{sec:ccamp}: a sparse forest is many small components.

\section{Definitions and conventions}\label{sec:defs}

Terms used throughout, fixed here once.

\begin{itemize}
  \item \textbf{Closure layer.} One block $\mathcal I_r$ of the canonical
  partition of the vertex set induced by the parametric solution: the vertices
  that enter the optimal closed set exactly when $\lambda$ crosses the
  breakpoint $\lambda_r$. ``Number of layers'' equals the number of
  breakpoints of $u(\lambda)$, so it measures how much parametric structure an
  instance has, and it bounds the number of iterations of every algorithm here.
  \item \textbf{Threshold} of a vertex: the breakpoint at which that vertex
  enters the optimal closed set; vertices with equal threshold form one layer.
  \item \textbf{Density $\varrho$.} The probability with which the generator
  adds each candidate arc. A forest with density $\varrho$ has $\varrho(n-1)$
  arcs and $n-\varrho(n-1)$ connected components in expectation, so
  $\varrho=1.0$ gives one spanning tree and $\varrho=0.3$ a forest of many
  small trees (\cref{fig:struct}).
  \item \textbf{Coefficient family.} The rule used to draw the profits and
  weights of an instance; six are used (\cref{sec:instances}). It controls the
  \emph{shape} of the ratios $p_i/w_i$ and hence how many layers appear.
  \item \textbf{Paired ratio.} The ratio of two algorithms' times
  \emph{on the same instance}. Reported as the median over instances, never as
  a ratio of two aggregates: pairing removes the instance-to-instance variation
  that would otherwise swamp the comparison.
  \item \textbf{IQR (interquartile range).} $Q_3-Q_1$: the width of the band
  containing the central half of the observations. It is to the median what the
  standard deviation is to the mean, but insensitive to the tails. Beside a
  median ratio, a large IQR signals that the population mixes regimes instead
  of scattering around one value -- exactly what happens across densities in
  \cref{sec:ccamp}.
  \item \textbf{Relative IQR} (\cref{sec:dispersion}): the IQR of the
  repetitions of one instance divided by that instance's median time, in
  percent. It quantifies measurement noise, not algorithmic variability.
  \item \textbf{Peak RSS} (resident set size): the maximum amount of physical
  RAM the process had mapped during the run, from
  \texttt{getrusage(RUSAGE\_SELF).ru\_maxrss}. See \cref{sec:setup} for the
  two caveats that make it usable only for single-algorithm runs.
  \item \textbf{Censored run.} A run killed by the time limit (300\,s) or the
  memory ceiling (8\,GiB), hence without a measured time. There are none in
  this study; the term appears only to state that.
  \item \textbf{Campaign.} One preregistered block of the sweep -- a set of
  instances plus the algorithms run on them (B, C, D, E, G).
  \item \textbf{Tree of a forest, and its size.} A connected component of the
  \emph{underlying undirected} graph, and its number of vertices. Orientations
  are irrelevant to this notion: a forest of in-trees and a forest of mixed
  orientations built with the same seed have the same trees. A vertex with no
  incident arc is a tree of size 1, called \emph{isolated}
  (\cref{sec:forestsize}).
  \item \textbf{SHA-256 checksum.} A 256-bit fingerprint of a file, computed
  with the standard SHA-2 hash function: any change to the file's bytes, even a
  single one, changes the fingerprint, and no one can construct a different
  file with the same fingerprint. We record it for every instance file
  (\texttt{instances/manifests/*.json}) and for every archive in the data
  release, so that a regenerated or downloaded instance can be proved
  byte-for-byte identical to the one measured here -- a reproducibility device,
  not a security one. It is unrelated to the 64-bit \texttt{sequence\_hash}
  used to compare algorithm \emph{outputs} (\cref{sec:validation}), which is a
  cheap non-cryptographic hash.
\end{itemize}

\section{Setup}\label{sec:setup}

\paragraph{Machine and build.} Intel Core i7-12700, 32\,GB RAM, Ubuntu
22.04.5 (kernel 6.8), GCC 11.4.0 at \texttt{-O3}, single-threaded. Each
benchmark process pinned to one physical core (\texttt{taskset}); the six
campaign lanes ran on distinct cores, never two on the same core. CPU
governor \texttt{powersave} (no passwordless root on this machine), so
absolute times carry ordinary frequency-scaling noise -- which is one reason
every comparison is a paired same-instance ratio.

\paragraph{What is timed.} Only the algorithm call
(\texttt{std::chrono::steady\_clock}); parsing, result hashing and correctness
comparison sit outside the timed region. Repetitions: 11 for random forests
with $n\le1\,000$, 3 elsewhere. Algorithm order shuffled per repetition, so no
algorithm is systematically favoured by the CPU frequency state.

\paragraph{Memory, and what ``peak RSS'' means.} RSS is the \emph{resident set
size}: the amount of physical RAM the process has mapped at a given instant.
Peak RSS is its maximum over the run, read from
\texttt{getrusage(RUSAGE\_SELF).ru\_maxrss}. Two properties matter when
reading memory figures. It is a \emph{process-lifetime} peak and never
decreases, so in an invocation that benchmarks several algorithms the value
recorded for the second includes the peak of the first -- memory is therefore
quoted only from single-algorithm invocations (\cref{sec:heap}). And it counts
pages actually resident, so it includes allocator overhead and is precisely
the quantity that decides whether a run survives a memory ceiling.

\paragraph{Statistics.} Absolute times: median over repetitions, then median
over instances. Comparisons: \emph{paired} per instance, reported as the
median of the per-instance ratios together with their IQR
(\cref{sec:defs}), never a ratio of aggregate means.
The median is chosen because it is invariant under inversion of the comparison
(the arithmetic mean of ratios is not: it depends on which algorithm sits in
the denominator), because the timing noise has an asymmetric right tail by
construction (interference can only slow a run down, never speed it up), and
because it is the conservative choice on every comparison where we claim an
advantage -- on the \texttt{BPPF} comparison at $n=1\,000$ the three
aggregates are $4.5$ (median), $5.8$ (geometric mean) and $8.7$ (arithmetic
mean).

\section{Instances}\label{sec:instances}

An instance of this problem is two independent things: a \emph{precedence
structure} (which vertex forces which) and a set of \emph{affine
coefficients} (a profit and a weight per vertex). We generate the two
separately and cross them, so that any effect observed can be attributed to
one or the other. Concretely, one instance is fully determined by four
choices: a topology, a coefficient family, a size $n$, and a random seed.
Nothing else is tuned -- in particular there is no capacity, no time horizon
and no scaling of the coefficients.

Files use the \texttt{.pcf} format: the arc list, one integer profit and one
strictly positive integer weight per vertex, where arc $(u,v)$ encodes the
implication $x_u\le x_v$. The format has no capacity field and no reader
accepts one -- a deliberate choice, since the same algorithms exist in the
literature for capacitated variants and we did not want the two to be
confusable. Every instance is written by a seeded deterministic Python
generator, never by hand, and is recorded in a manifest with its SHA-256
checksum, its topology classification and its coefficient bounds; a
regenerated or downloaded archive is therefore verifiable byte-for-byte
against the one used here.

Below, each of the two components is described with the rule that generates
it. \Cref{sec:layersize} then measures what the combination actually produces
-- how many closure layers, and how big -- which is the quantity that governs
how much work every algorithm has to do.

\paragraph{Coefficient families (six).}
\begin{itemize}
  \item \texttt{independent-positive}: $w_i,p_i$ independent uniform on
  $\{1,\dots,1000\}$.
  \item \texttt{independent-signed}: $w_i$ as above, $p_i$ uniform on
  $\{-1000,\dots,1000\}$.
  \item \texttt{correlated}: $p_i=w_i+\delta_i$, $\delta_i$ uniform on
  $\{-50,\dots,50\}$.
  \item \texttt{anti-correlated}: $p_i=(1001-w_i)+\delta_i$.
  \item \texttt{near-ties}: one target ratio $\mathrm{num}/\mathrm{den}$ per
  instance ($\mathrm{num},\mathrm{den}$ uniform on $\{1,\dots,20\}$), then
  $t_i$ uniform on $\{1,\dots,50\}$, $w_i=\mathrm{den}\,t_i$,
  $p_i=\mathrm{num}\,t_i+\jmath_i$ with jitter $\jmath_i\in\{-2,-1,1,2\}$.
  \item \texttt{exact-ties}: $g=\max(1,\lfloor n/20\rfloor)$ groups each with
  its own exact ratio; vertex $i$ goes to group $i\bmod g$, so equal-threshold
  vertices are spread over the graph rather than adjacent.
\end{itemize}

\paragraph{Topologies (six).} \texttt{mixed-forest}: each $v\ge2$ linked to a
uniform predecessor $u<v$ with probability $\varrho$, each edge oriented
$(u,v)$ or $(v,u)$ with probability $1/2$. \texttt{in-forest}: one arc
$(u,j)$ with $j$ uniform above $u$, with probability $\varrho$ (out-degree
$\le1$); \texttt{out-forest} symmetric (in-degree $\le1$).
\texttt{path-mixed}, \texttt{binary-mixed}, \texttt{star-mixed}: fixed
underlying shape (path; balanced binary tree, parent
$\lfloor(v-1)/2\rfloor$; star, hub $0$), orientations randomized.

\paragraph{Sizes and counts.} 240 instances per topology and size (four
densities $\times$ six families $\times$ ten seeds), 60 for the structured
families, which have no density parameter. The random topologies use the ten
sizes $100,200,\dots,1\,000$ and the ten sizes
$10\,000,20\,000,\dots,100\,000$; the structured families use
$100$, $200$, $500$, $1\,000$, $2\,000$, $5\,000$, $10\,000$, $20\,000$,
$50\,000$ and $100\,000$.

\tabfig{fig:struct}{%
Median number of arcs, of trees and of closure layers of the generated
instances, per arc density. Instances: \texttt{mixed-forest},
$n\in\{10\,000,\dots,100\,000\}$ pooled, 600 instances per density.
Observations: what the density parameter actually produces. Arcs grow linearly in
$\varrho$ and the number of trees falls accordingly, from
$\approx38\,500$ trees at $\varrho=0.3$ to a single spanning tree at
$\varrho=1.0$. The number of closure layers moves the other way and far less:
connecting the forest removes only about a third of them, because precedence
constraints merge layers that would otherwise be distinct. This is why density
is a genuine experimental factor and not a cosmetic one.}{%
\tab{rep_struct_by_rho}}{%
\begin{tikzpicture}
\begin{axis}[rhoaxis, ylabel={count}, ymode=log]
\addplot[q1] table[x=rho, y=arcs]   {\dfile{rep_plotstruct}}; \addlegendentry{arcs}
\addplot[q2] table[x=rho, y=comp]   {\dfile{rep_plotstruct}}; \addlegendentry{trees}
\addplot[q3] table[x=rho, y=layers] {\dfile{rep_plotstruct}}; \addlegendentry{closure layers}
\end{axis}
\end{tikzpicture}}

\subsection{How many trees, and how big?}\label{sec:forestsize}

\Cref{fig:struct} counts the trees of the generated forests; this subsection
measures how large they are, because ``sparse forest'' can mean two very
different structures -- many trees of comparable size, or one giant tree
surrounded by dust -- and the two would explain \texttt{RaC}'s density
sensitivity (\cref{sec:ccamp}) in different ways. The measurement is made
directly on the \texttt{.pcf} files by \texttt{tools/forest\_structure\_stats.py},
without running any algorithm: the trees are a property of the input.

Two closed-form facts anchor the numbers. The generator adds each of the $n-1$
candidate arcs independently with probability $\varrho$ and never creates a
cycle, so a forest has $\varrho(n-1)$ arcs and therefore exactly
$n-\varrho(n-1)$ trees in expectation: the tree count is $(1-\varrho)n$ to
first order, and the \emph{mean} tree size is $1/(1-\varrho)$. Both are
independent of $n$ -- so density, not size, is what sets the fragmentation, and
the same density means the same fragmentation at $n=10^3$ and at $n=10^5$.

\tabfigwide{fig:forest}{%
Structure of the generated forests: median number of arcs, median number of
trees, trees per vertex, then the median, mean and largest tree size, the
largest tree as a percentage of $n$, and the percentage of trees that are
isolated vertices; the plot below the table shows the three quantities that move
with the density, at $n=100\,000$. Instances: \texttt{mixed-forest} at
$n\in\{10^3,10^4,10^5\}$, \texttt{in-forest} and \texttt{out-forest} at
$n=10^5$, all four densities, plus the three structured classes at $n=10^5$;
10 instances per row, all with \texttt{independent-positive} coefficients, since
the topology is generated independently of the coefficients and one family
therefore represents all six. Observations: measured
values match the closed forms above to three digits -- $(1-\varrho)n$ trees, mean
size $1/(1-\varrho)$ -- and are the same for \texttt{mixed-forest},
\texttt{in-forest} and \texttt{out-forest}, confirming that orientation does not
touch the structure; \texttt{path-}, \texttt{binary-} and \texttt{star-mixed}
are single trees by construction. The interesting column is the largest tree:
at $\varrho=0.3$ and $\varrho=0.6$ it holds $0.1\%$ and $1.6\%$ of the vertices
at $n=10^5$, but at $\varrho=0.9$ it holds $36\%$ -- while the median tree is
\emph{still} a single vertex and half the trees are still isolated vertices.}{%
\tiny\setlength{\tabcolsep}{3pt}\tab{rep_forest_struct}}{%
\begin{tikzpicture}
\begin{axis}[rhoaxis, ylabel={count / size}, ymode=log, width=0.62\textwidth]
\addplot[q1] table[x=rho, y=trees]   {\dfile{rep_plotforest}}; \addlegendentry{\#trees}
\addplot[q2] table[x=rho, y=mean]    {\dfile{rep_plotforest}}; \addlegendentry{mean tree size}
\addplot[q3] table[x=rho, y=largest] {\dfile{rep_plotforest}}; \addlegendentry{largest tree}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:forestccdf}{%
Distribution of the tree size: for each size $s$, the percentage of trees with
at least $s$ vertices, one curve and one column per density; the table lists the
same distribution at selected sizes, plus the largest tree observed. Instances:
\texttt{mixed-forest}, $n=100\,000$, 10 instances per density, 40 in total.
Observations: the distribution is heavy-tailed
at every density below 1: at $\varrho=0.3$, $77\%$ of the trees are single
vertices and only $0.3\%$ reach 10 vertices, yet the largest reaches 96. At
$\varrho=0.9$ the tail detaches completely -- $30\%$ of trees have $\ge3$
vertices and one tree of $\approx4\cdot10^4$ vertices appears, four orders of
magnitude above the median. This is the percolation-like transition that
\cref{fig:c_ratio_rho} sees as a timing effect: what changes between
$\varrho=0.6$ and $\varrho=0.9$ is not the number of trees (it falls smoothly)
but the emergence of one giant tree, and \texttt{RaC}'s per-component overhead
is paid on the count of trees while its contraction rounds are amortized over
the giant one.}{%
\tiny\setlength{\tabcolsep}{3pt}\tab{rep_forest_ccdf}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, ymode=log, sizeaxis, xlabel={tree size $s$},
  ylabel={\% of trees with size $\ge s$}, legend columns=2]
\addplot[d1] table[x=size, y=r03] {\dfile{rep_plotforestccdf}};
\addlegendentry{$\varrho=0.3$}
\addplot[d2] table[x=size, y=r06] {\dfile{rep_plotforestccdf}};
\addlegendentry{$\varrho=0.6$}
\addplot[d3] table[x=size, y=r09] {\dfile{rep_plotforestccdf}};
\addlegendentry{$\varrho=0.9$}
\addplot[d4] table[x=size, y=r10] {\dfile{rep_plotforestccdf}};
\addlegendentry{$\varrho=1.0$}
\end{axis}
\end{tikzpicture}}

\subsection{How many closure layers?}\label{sec:layercount}

The number of closure layers is the number of breakpoints, hence an upper bound
on the number of iterations of every peeling algorithm: it is the single best
predictor of absolute running time. The two figures below report it per
coefficient family and per size.

\tabfig{fig:layersfam}{%
Median number of closure layers per coefficient family, reported separately for
the small and the large sizes. Instances: \texttt{mixed-forest}, all four
densities, $n\in\{100,\dots,1\,000\}$ (2\,400 instances) and
$n\in\{10\,000,\dots,100\,000\}$ (2\,400 instances). Observations: how much
parametric structure each family creates -- the single most useful fact for
reading absolute times, since the work of every algorithm scales with the number
of breakpoints. At $n\ge10^4$ the median layer count ranges from $1\,222$
(\texttt{near-ties}) to $33\,183$ (\texttt{independent-signed}), a factor of 27 at
equal size. This is why the tie-stressing families are the \emph{fastest} in
every timing table below, which would otherwise look counter-intuitive.}{%
\tab{rep_layers_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={\# closure layers}, ymode=log]
\addplot[q1, only marks, mark size=2.6pt] coordinates {(1,344) (2,346) (3,337) (4,342) (5,164) (6,105)};
\addlegendentry{$n\le10^3$}
\addplot[q3, only marks, mark size=2.6pt] coordinates {(1,32374) (2,33183) (3,24825) (4,30379) (5,1222) (6,7884)};
\addlegendentry{$n\ge10^4$}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:layerssize}{%
Median, minimum and maximum number of closure layers, per size. Instances:
\texttt{mixed-forest}, $n$ from $100$ to $100\,000$, all four densities and six
families, 240 instances per size. Observations: the median layer count grows
essentially linearly in $n$, settling at $0.43$ layers per vertex
($43\,390$ layers at $n=10^5$): a $10^5$-vertex instance typically has of the
order of $4\cdot10^4$ breakpoints to enumerate. The min--max band, however, spans
a factor of $86$ at that size ($915$ to $78\,393$), and the reason is not noise
but the coefficient families: \texttt{near-ties} produces $27\times$ fewer
layers than the independent families (\cref{fig:layersfam}). Two instances of
equal size can therefore carry very different amounts of parametric structure,
which is the dominant source of instance-to-instance variation in every timing
table below.}{%
\tab{rep_layers_by_size}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, ymode=log, sizeaxis, ylabel={\# closure layers}]
\addplot[q1] table[x=n, y=median] {\dfile{rep_plotlayers}}; \addlegendentry{median}
\addplot[q3] table[x=n, y=max]    {\dfile{rep_plotlayers}}; \addlegendentry{max}
\addplot[q2] table[x=n, y=min]    {\dfile{rep_plotlayers}}; \addlegendentry{min}
\end{axis}
\end{tikzpicture}}

\subsection{How big are the closure layers?}\label{sec:layersize}

The campaign CSVs record how \emph{many} layers an instance has, not how big
they are: the algorithms are timed and their output is discarded. The size
distribution is worth one measurement of its own, because it is what a peel
step actually consumes. \Cref{fig:layersize} reports it on a sample of 60
instances at $n=10\,000$ (all six families, four densities, plus stars, paths
and in-forests), 261\,232 layers in total.

\tabfigwide{fig:layersize}{%
Distribution of the closure-layer \emph{size} (number of vertices per layer):
the table gives the number of layers, their median, mean and maximum size and the
percentage of singleton layers per group; the plot gives the percentage of layers
of size at least $s$. Instances: 60 instances at $n=10\,000$ -- all six
coefficient families and four densities of \texttt{mixed-forest}, plus
\texttt{star-mixed}, \texttt{path-mixed} and \texttt{in-forest} -- for a total of
261\,232 layers. Observations: \emph{the distribution is extremely skewed}: overall
median 1, mean $2.3$, and two thirds of all layers are singletons -- yet the
largest single layer holds $5\,077$ vertices, half the graph. So a parametric
solution is typically a long chain of one-vertex layers plus a handful of very
large blocks, and an algorithm's cost per layer matters more than its cost per
vertex. Two group-level facts stand out. The tie-stressing families behave
completely differently from the others: \texttt{near-ties} has median layer
size 7 and \emph{no} singletons at all (by construction, vertices share
thresholds), against median 1 and $73$--$75\%$ singletons for the independent
families -- which is the mechanism behind their much smaller layer counts
(\cref{fig:layersfam}). And density compresses the largest layer by an order
of magnitude: on sparse random forests the biggest layer holds $563$ vertices,
against $\approx5\,000$ once the graph is a single tree, because a layer cannot
straddle components.}{%
\scriptsize\setlength{\tabcolsep}{3pt}\tab{rep_layer_sizes}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, ymode=log, sizeaxis, xlabel={layer size $s$},
  ylabel={\% of layers with size $\ge s$}, width=0.62\textwidth]
\addplot[q1, mark=none, very thick] table[x=size, y=frac_at_least]
  {\dfile{rep_plotlayersize}};
\addlegendentry{all 261\,232 layers}
\end{axis}
\end{tikzpicture}}

\section{Validation}\label{sec:validation}

\begin{itemize}
  \item \textbf{Exhaustive enumeration oracle} (CTest): every directed forest
  with $\le4$ vertices over a finite coefficient grid; 10\,000 random forests
  up to 11 vertices; mixed-orientation path, binary and star trees up to 257
  vertices; 6\,000 in-forest and 6\,000 out-forest instances plus 2\,000
  larger ones per orientation; plus the structural invariants (layers
  partition $V$, each is a closure increment, thresholds strictly decreasing).
  \item \textbf{Cross-algorithm differential agreement}: every campaign runs
  several algorithms on the same instance and compares partition, thresholds
  and canonical order through a 64-bit fingerprint. Over the sweep:
  \emph{zero} disagreements (\cref{tab:correctness}).
  \item \textbf{Sanitizers and CI}: GCC Release, GCC Debug with the address
  and undefined-behaviour sanitizers, and Clang Debug; all three pass at the
  frozen commit (\path{results/TEST_REPORT_2026-08-31.md}).
  \item \textbf{Overflow}: \texttt{validate\_instance} refuses any instance
  whose total absolute profit or weight would leave the exact-arithmetic
  margin ($\texttt{INT64\_MAX}/4$), before any algorithm runs.
\end{itemize}

\begin{table}[htbp]
\centering
\caption{Number of instances benchmarked, of timed runs, and of
cross-algorithm disagreements, per instance class. Every group aggregates all
sizes, densities, coefficient families and seeds of that class. The
\texttt{BPPF} comparison is listed apart, \texttt{BPPF} being an external
baseline rather than one of our algorithms. Zero disagreements everywhere: on
each instance the algorithms returned the same partition, the same thresholds and
the same canonical order.}
\label{tab:correctness}
\small
\tab{rep_correctness}
\end{table}

\section{The campaigns}\label{sec:campaigns}

A \emph{campaign} is one block of the sweep: a set of instances, the
algorithms run on them, and the number of repetitions. The split is not
cosmetic -- each campaign answers one question, and keeping them separate is
what allows a result to be attributed to a cause:

\begin{itemize}
  \item \textbf{A} is correctness only, on instances small enough to be
  solved by brute force, so that every algorithm can be checked against an
  independent oracle rather than against another algorithm.
  \item \textbf{B} and \textbf{C} are the same random topology at two scales.
  B is where all five algorithms still fit, so it is the one place where every
  pairwise comparison is available; C drops the two quadratic ones and asks how
  the rest scale.
  \item \textbf{D} fixes the shape (path, balanced binary tree, star) and
  varies nothing but orientations and coefficients, which isolates the effect
  of topology -- and produces the one case where the theoretical separation
  between our algorithms becomes visible.
  \item \textbf{E} restricts the orientation (in-forest, out-forest) to
  measure what a specialized algorithm gains from knowing it in advance.
  \item \textbf{G} is the external comparison against parametric pseudoflow.
  It sits on the same instances as B, so its numbers can be read against the
  rest of the study.
\end{itemize}

\noindent
All campaigns were frozen in \texttt{docs/EXPERIMENTAL\_PLAN\_V3.md} before any
run, together with the two size cutoffs, so that no scoping decision was taken
after seeing results. \Cref{tab:campaigns} gives the definitions,
\cref{tab:runs} how much work each algorithm actually did, and
\cref{tab:correctness} the correctness outcome per campaign.

\begin{table}[htbp]
\centering
\caption{Campaign definitions. Every campaign uses all six coefficient
families and ten seeds; the random topologies use all four densities. Two
preregistered size cutoffs apply, in both cases because the asymptotic trend
is already established below them and running further would consume hours
without adding information: \texttt{PaC}/\texttt{DPaC} stop at $n=20\,000$ on
random forests, and \texttt{HPaC}/\texttt{DHPaC} stop at $n=20\,000$ on stars
(where \texttt{PaC} is cheap and runs the full range instead).}
\label{tab:campaigns}
\begin{adjustbox}{max width=\textwidth}
\small
\begin{tabular}{@{}llllrl@{}}
\toprule
& Topology & Sizes $n$ & Inst./size & Reps & Algorithms \\
\midrule
A & all six & $\le11$ (exhaustive at 4) & -- & -- &
all, against the enumeration oracle \\
B & \texttt{mixed-forest} & $100,200,\dots,1\,000$ & 240 & 11 &
\texttt{PaC}, \texttt{DPaC}, \texttt{HPaC}, \texttt{DHPaC}, \texttt{RaC} \\
C & \texttt{mixed-forest} & $10\,000,\dots,100\,000$ & 240 & 3 &
\texttt{HPaC}, \texttt{DHPaC}, \texttt{RaC}; \texttt{PaC}, \texttt{DPaC} at
$10^4$, $2\cdot10^4$ \\
D & \texttt{path-}, \texttt{binary-}, \texttt{star-mixed} & 10 sizes,
$100\dots100\,000$ & 60 & 3 &
\texttt{HPaC}, \texttt{RaC}, \texttt{DHPaC}, \texttt{PaC}, \texttt{DPaC} \\
E & \texttt{in-}, \texttt{out-forest} & 20 sizes,
$100\dots100\,000$ & 240 & 3 &
\texttt{HPaC}, \texttt{DHPaC}, \texttt{HIPaC}/\texttt{HOPaC}, \texttt{RaC} \\
G & \texttt{mixed-forest} & $100,200,\dots,1\,000$ & 240 & 3 &
\texttt{HPaC} against \texttt{BPPF} \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

\begin{table}[htbp]
\centering
\caption{Work actually done per algorithm over the sweep. The
runs-per-instance column reflects the mix of repetition counts and cutoffs:
\texttt{PaC} and \texttt{DPaC} appear on fewer instances (size cutoffs) but
with more repetitions each, since they were mostly exercised on the medium
campaign where 11 repetitions were used. \texttt{RaC} appears on the most
instances, being the only algorithm run on every class at every size.}
\label{tab:runs}
\small
\tab{rep_runs_per_algorithm}
\end{table}

\section{Random forests}\label{sec:random}

The two random campaigns answer two different questions. At medium sizes the
question is whether the heap is worth its maintenance cost
(\cref{sec:bcamp}); at large sizes it is how the three surviving algorithms
scale, and how much the answer depends on the density of the forest
(\cref{sec:ccamp}).

\subsection{Medium sizes: direct scan versus heap}\label{sec:bcamp}

Campaign B runs all five of our algorithms on the same 2\,400 instances, which
makes it the one place where the primal/dual and scan/heap choices can be
compared without any confounding factor. It is also the size range where the
heap's overhead is still comparable to its benefit, so it is where the
crossover can be located.

\tabfig{fig:b}{%
Median CPU time in milliseconds of the five algorithms, per size. Instances:
\texttt{mixed-forest}, $n\in\{100,200,\dots,1\,000\}$, 240 instances per size
(four densities $\times$ six coefficient families $\times$ ten seeds), 11
repetitions each. Observations: the scan-based and the heap-based algorithms are
indistinguishable up to $n\approx300$ and the heap pays off from $n\approx400$
on; each dual tracks its primal over the whole range, which is why the
manuscript reports only the primals. The mechanism is the candidate selection:
\texttt{PaC} re-scans every candidate ratio at every iteration, while
\texttt{HPaC} extracts the maximum in logarithmic time and refreshes only the
candidates touched by the last peel or contraction.}{%
\tab{rep_times_campaign_b}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, ymode=log, sizeaxis, ylabel={CPU time (ms)}]
\addplot[pac]   table[x=n, y=pac]   {\dat{campaign_b}}; \addlegendentry{\texttt{PaC}}
\addplot[dpac]  table[x=n, y=dpac]  {\dat{campaign_b}}; \addlegendentry{\texttt{DPaC}}
\addplot[hpac]  table[x=n, y=hpac]  {\dat{campaign_b}}; \addlegendentry{\texttt{HPaC}}
\addplot[dhpac] table[x=n, y=dhpac] {\dat{campaign_b}}; \addlegendentry{\texttt{DHPaC}}
\addplot[rac]   table[x=n, y=rac]   {\dat{campaign_b}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:b_ratio_pac}{%
Median of the paired per-instance ratio \texttt{PaC}/\texttt{HPaC}, per size,
with the number of paired instances and the IQR of the ratio. Instances:
\texttt{mixed-forest}, $n\in\{100,200,\dots,1\,000\}$, 240 instances per size.
Observations: the ratio is $0.91$ at $n=100$ -- the scan is marginally faster
where heap maintenance does not yet pay for itself -- crosses 1 near $n=400$ and
reaches $2.3$ at $n=1\,000$. Beyond this range it keeps growing, $20.3$ at
$n=10^4$ and $42.9$ at $n=2\cdot10^4$ (\cref{sec:ccamp}), which is the practical
meaning of the $\mathcal O(n^2)$ bound against $\mathcal O(n\log n)$ behaviour in
practice. The dashed line marks parity.}{%
\tab{rep_ratio_campaign_b_pac_over_hpac_by_size}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, pcfplot, xlabel={$n$}, ylabel={\texttt{PaC}/\texttt{HPaC}},
  ymin=0,
  extra y ticks={1}, extra y tick style={grid=major, grid style={black!45, dashed}}]
\addplot[pac] table[x=n, y expr=\thisrow{pac}/\thisrow{hpac}] {\dat{campaign_b}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:b_rho}{%
Median CPU time in milliseconds of \texttt{PaC}, \texttt{HPaC} and \texttt{RaC},
per arc density. Instances: \texttt{mixed-forest}, $n\in\{100,\dots,1\,000\}$
pooled, 600 instances per density. Observations: every algorithm slows down as
the forest becomes more connected -- more arcs mean more closure sums to
propagate -- but by very different factors from $\varrho=0.3$ to
$\varrho=1.0$: $4.5\times$ for \texttt{HPaC} ($0.2$ to $0.9$\,ms),
$3.8\times$ for \texttt{RaC} ($0.6$ to $2.3$\,ms) and only $1.5\times$ for
\texttt{PaC} ($0.6$ to $0.9$\,ms), which maintains nothing and therefore has
nothing to refresh when the forest gains arcs. \texttt{HPaC} overtakes
\texttt{PaC} on sparse forests and merely draws level with it on single spanning
trees: the heap's advantage at these sizes comes from the sparse end. This is the
medium-size beginning of the density effect that dominates \cref{sec:ccamp}.}{%
\tab{rep_times_campaign_b_by_rho}}{%
\begin{tikzpicture}
\begin{axis}[rhoaxis, ylabel={CPU time (ms)}]
\addplot[pac]  table[x=rho, y=pac]  {\dfile{rep_plotrho_campaign_b}}; \addlegendentry{\texttt{PaC}}
\addplot[hpac] table[x=rho, y=hpac] {\dfile{rep_plotrho_campaign_b}}; \addlegendentry{\texttt{HPaC}}
\addplot[rac]  table[x=rho, y=rac]  {\dfile{rep_plotrho_campaign_b}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:b_fam}{%
Median CPU time in milliseconds of \texttt{PaC}, \texttt{HPaC} and \texttt{RaC},
per coefficient family. Instances: \texttt{mixed-forest},
$n\in\{100,\dots,1\,000\}$ and all four densities pooled, 400 instances per
family. Observations: the two tie-stressing families are the
fastest for \texttt{PaC} ($0.5$--$0.6$ against $0.8$\,ms) and for \texttt{HPaC}
($0.3$ against $0.4$\,ms), consistently with their smaller layer counts
(\cref{fig:layersfam}) -- fewer breakpoints, fewer iterations -- while for
\texttt{RaC} the six families all sit at $1.2$--$1.3$\,ms, indistinguishable at
this resolution. The ordering between algorithms is the same in all six
families, so no family reverses a conclusion.}{%
\tab{rep_times_campaign_b_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={CPU time (ms)}]
\addplot[pac]  table[x=idx, y=pac]  {\dfile{rep_plotfam_campaign_b}}; \addlegendentry{\texttt{PaC}}
\addplot[hpac] table[x=idx, y=hpac] {\dfile{rep_plotfam_campaign_b}}; \addlegendentry{\texttt{HPaC}}
\addplot[rac]  table[x=idx, y=rac]  {\dfile{rep_plotfam_campaign_b}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:b_ratio_fam}{%
Median of the paired per-instance ratio \texttt{RaC}/\texttt{HPaC}, per
coefficient family, with the number of paired instances and the IQR. Instances:
\texttt{mixed-forest}, $n\in\{100,\dots,1\,000\}$ and all four densities pooled,
400 instances per family. Observations: the ratio is flat at $3.1$--$3.3$ across
all six families -- at these sizes the coefficient family changes how much work
there is, not which algorithm does it faster.}{%
\tab{rep_ratio_campaign_b_rac_over_hpac_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={\texttt{RaC}/\texttt{HPaC}}, ymin=0]
\addplot[rac] table[x=idx, y=ratio] {\dfile{rep_plotratiofam_campaign_b_rac_hpac}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\subsection{Large sizes, and the effect of density}\label{sec:ccamp}

Campaign C drops the two direct-scan algorithms beyond $n=20\,000$ (their
quadratic growth is established by then) and asks the question that matters at
scale: how do \texttt{HPaC}, its dual and \texttt{RaC} compare on forests with
up to $10^5$ vertices, and how much of the answer depends on the density of
the forest. As it turns out, more than it depends on the size.

\tabfig{fig:c}{%
Median CPU time in milliseconds of \texttt{HPaC}, \texttt{DHPaC} and
\texttt{RaC}, per size. Instances: \texttt{mixed-forest},
$n\in\{10\,000,20\,000,\dots,100\,000\}$, 240 instances per size (four densities
$\times$ six families $\times$ ten seeds), 3 repetitions each. Observations:
\texttt{HPaC} computes the full parametric solution on $10^5$ vertices in about
$0.2$\,s ($203.9$\,ms). The paired ratio \texttt{DHPaC}/\texttt{HPaC} stays
between $0.96$ and $1.24$ over the range, so the primal/dual choice is immaterial
at scale -- the difference is small enough that the median columns of the table
and the paired ratio need not rank the two the same way at $n=10^5$.
\texttt{RaC} is above both at every size, by a factor that grows with $n$
(\cref{fig:c_ratio}).}{%
\tab{rep_times_campaign_c}}{%
\begin{tikzpicture}
\begin{axis}[pcfplot, xlabel={$n$}, ylabel={CPU time (ms)},
  xtick={10000,40000,70000,100000}, scaled x ticks=false,
  /pgf/number format/.cd, fixed, 1000 sep={}, /tikz/.cd]
\addplot[rac]   table[x=n, y=rac]   {\dat{campaign_c}}; \addlegendentry{\texttt{RaC}}
\addplot[dhpac] table[x=n, y=dhpac] {\dat{campaign_c}}; \addlegendentry{\texttt{DHPaC}}
\addplot[hpac]  table[x=n, y=hpac]  {\dat{campaign_c}}; \addlegendentry{\texttt{HPaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:c_ratio}{%
Median of the paired per-instance ratio \texttt{RaC}/\texttt{HPaC}, per size,
with the number of paired instances and the IQR. Instances:
\texttt{mixed-forest}, $n\in\{10\,000,\dots,100\,000\}$, 240 instances per size.
Observations: the ratio grows from $4.2$ at $n=10^4$ to $13.3$ at $n=10^5$, and
its IQR widens with $n$ as well, so \texttt{RaC}'s disadvantage grows \emph{and}
becomes more variable. The variability is not noise: it is the mixture of the
four densities, which \cref{fig:c_ratio_rho} separates.}{%
\tab{rep_ratio_campaign_c_rac_over_hpac_by_size}}{%
\begin{tikzpicture}
\begin{axis}[pcfplot, xlabel={$n$}, ylabel={\texttt{RaC}/\texttt{HPaC}},
  xtick={10000,40000,70000,100000}, scaled x ticks=false, ymin=0,
  /pgf/number format/.cd, fixed, 1000 sep={}, /tikz/.cd]
\addplot[rac] table[x=n, y expr=\thisrow{rac}/\thisrow{hpac}] {\dat{campaign_c}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:c_ratio_rho}{%
Median of the paired per-instance ratio \texttt{RaC}/\texttt{HPaC}, per arc
density, with the number of paired instances and the IQR. Instances:
\texttt{mixed-forest}, $n\in\{10\,000,\dots,100\,000\}$ pooled, 600 instances per
density. Observations: the ratio is $12.9$ at $\varrho=0.3$, $17.0$ at
$\varrho=0.6$, $5.0$ at $\varrho=0.9$ and only $2.0$ on single spanning trees,
with the IQR shrinking from $10.2$ to $0.4$ in the same direction. A sparse
forest splits into tens of thousands of tiny trees (\cref{fig:forest}), on which
\texttt{HPaC}'s contractions are almost free while \texttt{RaC} still pays
cluster bookkeeping per tree; on one spanning tree the two are closest. The
ordering never reverses, but the margin varies by almost an order of magnitude --
the population is a mixture of four regimes, which is exactly what the widening
IQR of \cref{fig:c_ratio} is made of, and the reason a single aggregate over
densities would mislead.}{%
\tab{rep_ratio_campaign_c_rac_over_hpac_by_rho}}{%
\begin{tikzpicture}
\begin{axis}[rhoaxis, ylabel={\texttt{RaC}/\texttt{HPaC}}, ymin=0]
\addplot[rac] table[x=rho, y=ratio] {\dfile{rep_plotratiorho_campaign_c_rac_hpac}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:c_rho}{%
Median CPU time in milliseconds of \texttt{RaC}, \texttt{DHPaC} and
\texttt{HPaC}, per arc density. Instances: \texttt{mixed-forest},
$n\in\{10\,000,\dots,100\,000\}$ pooled, 600 instances per density.
Observations: this is \cref{fig:c_ratio_rho} in absolute terms, and the two
algorithms respond to density in opposite ways. \texttt{HPaC} grows
monotonically, by a factor 8 from $\varrho=0.3$ to $\varrho=1.0$ ($27.8$ to
$221.4$\,ms): more arcs mean more closure sums to propagate per move.
\texttt{RaC} is \emph{non-monotone} -- it peaks at $\varrho=0.6$ ($902$\,ms) and
falls to $433$\,ms on single spanning trees -- because its cost is a per-tree
overhead plus contraction work per tree: at $\varrho=0.3$ there are $\approx
70\,000$ trees but $77\%$ are isolated vertices and cost nothing to contract, at
$\varrho=0.6$ there are still $40\,000$ trees and most are non-trivial (the worst
combination), and from $\varrho=0.9$ on the count collapses and the work
concentrates in one large tree, where the $\mathcal O(\log n)$ contraction rounds
are amortized best (\cref{fig:forest,fig:forestccdf}).}{%
\tab{rep_times_campaign_c_by_rho}}{%
\begin{tikzpicture}
\begin{axis}[rhoaxis, ylabel={CPU time (ms)}]
\addplot[rac]   table[x=rho, y=rac]   {\dfile{rep_plotrho_campaign_c}}; \addlegendentry{\texttt{RaC}}
\addplot[dhpac] table[x=rho, y=dhpac] {\dfile{rep_plotrho_campaign_c}}; \addlegendentry{\texttt{DHPaC}}
\addplot[hpac]  table[x=rho, y=hpac]  {\dfile{rep_plotrho_campaign_c}}; \addlegendentry{\texttt{HPaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:c_fam}{%
Median CPU time in milliseconds of \texttt{RaC}, \texttt{DHPaC} and
\texttt{HPaC}, per coefficient family. Instances: \texttt{mixed-forest},
$n\in\{10\,000,\dots,100\,000\}$ and all four densities pooled, 400 instances per
family. Observations: the spread across the six families is
$14\%$ for \texttt{HPaC} ($62.0$--$70.7$\,ms), $16\%$ for \texttt{DHPaC} and
$15\%$ for \texttt{RaC} ($490$--$563$\,ms) -- an order of magnitude less than
the spread across densities in \cref{fig:c_rho}, and in the same direction for
all three algorithms, \texttt{near-ties} and \texttt{exact-ties} being the
fastest for each of them.}{%
\tab{rep_times_campaign_c_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={CPU time (ms)}]
\addplot[rac]   table[x=idx, y=rac]   {\dfile{rep_plotfam_campaign_c}}; \addlegendentry{\texttt{RaC}}
\addplot[dhpac] table[x=idx, y=dhpac] {\dfile{rep_plotfam_campaign_c}}; \addlegendentry{\texttt{DHPaC}}
\addplot[hpac]  table[x=idx, y=hpac]  {\dfile{rep_plotfam_campaign_c}}; \addlegendentry{\texttt{HPaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:c_ratio_fam}{%
Median of the paired per-instance ratio \texttt{RaC}/\texttt{HPaC}, per
coefficient family, with the number of paired instances and the IQR. Instances:
\texttt{mixed-forest}, $n\in\{10\,000,\dots,100\,000\}$ and all four densities
pooled, 400 instances per family. Observations: the ratio lies between $5.80$ and
$6.11$ in all six families, a $5\%$ spread. Read against \cref{fig:c_ratio_rho},
where the same ratio moves between $2.0$ and $17.0$, this is the clearest single
statement of the study: the topology decides which algorithm wins, the
coefficient family only how much work there is to do.}{%
\tab{rep_ratio_campaign_c_rac_over_hpac_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={\texttt{RaC}/\texttt{HPaC}}, ymin=0]
\addplot[rac] table[x=idx, y=ratio] {\dfile{rep_plotratiofam_campaign_c_rac_hpac}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\subsection{What the random campaigns say}

Three things follow from \cref{fig:b,fig:b_ratio_pac,fig:c,fig:c_ratio,fig:c_ratio_rho,fig:c_ratio_fam}
and are worth stating explicitly, because none of them is visible from the
complexity bounds alone.

\begin{itemize}
  \item \textbf{The heap is worth it, but only past a size threshold.} Below
  $n\approx300$ the direct scan is as good and at $n=100$ marginally better:
  the heap's maintenance is not amortized yet. The threshold is small enough
  that it does not matter in practice, but it does mean that for tiny
  instances the simpler algorithm is the right choice.
  \item \textbf{Density is a first-order factor, and it acts against
  \texttt{RaC}.} The \texttt{RaC}/\texttt{HPaC} ratio moves from $2.0$ to
  $17.0$ as $\varrho$ goes from $1.0$ to $0.6$ -- a bigger swing than going
  from $n=10^4$ to $n=10^5$ at fixed density. Anyone benchmarking these
  algorithms on a single density would draw a substantially wrong conclusion
  about the margin. The reason is structural: \texttt{RaC} pays cluster
  bookkeeping per component, and a sparse forest is thousands of tiny
  components.
  \item \textbf{The coefficient family is a second-order factor.} It changes
  absolute times by at most $14\%$ and the paired ratio by less than $6\%$,
  even though it changes the number of closure layers by a factor of 27
  (\cref{fig:layersfam}). Time per layer, not time per vertex, is what the
  family moves -- and it moves it for all algorithms together.
\end{itemize}

\section{Structured classes}\label{sec:structured}

Paths, balanced binary trees and stars share one property that the random
forests do not: the topology is fixed, so only the orientations and the
coefficients vary. They therefore isolate the effect of the shape, and they
are the classes where the theoretical complexity separation becomes visible.
Paths and binary trees confirm the random-forest picture; stars invert it.

\tabfig{fig:path}{%
Median CPU time in milliseconds of \texttt{PaC}, \texttt{HPaC}, \texttt{DHPaC}
and \texttt{RaC}, per size. Instances: \texttt{path-mixed} (one path, random
orientations), $n$ from $100$ to $100\,000$, 60 instances per size (six
coefficient families $\times$ ten seeds); \texttt{PaC} up to $n=2\,000$, its
preregistered cutoff. Observations: paths behave like random forests -- median
\texttt{RaC}/\texttt{HPaC} between $5.8$ and $9.0$ over the whole range, flat in
$n$, and \texttt{PaC} left far behind by $n=10^4$ as its $\mathcal O(n^2)$ bound
predicts. A path gives \texttt{RaC}'s compress rounds no branching to exploit
while every vertex has degree $\le2$, so no heap update ever cascades: the shape
favours the heap.}{%
\tab{rep_times_campaign_d_path}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, ymode=log, sizeaxis, ylabel={CPU time (ms)}]
\addplot[pac]  table[x=n, y=pac]  {\dat{campaign_d_path}}; \addlegendentry{\texttt{PaC}}
\addplot[hpac] table[x=n, y=hpac] {\dat{campaign_d_path}}; \addlegendentry{\texttt{HPaC}}
\addplot[dhpac] table[x=n, y=dhpac] {\dat{campaign_d_path}}; \addlegendentry{\texttt{DHPaC}}
\addplot[rac]  table[x=n, y=rac]  {\dat{campaign_d_path}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:path_fam}{%
Median CPU time in milliseconds of \texttt{PaC}, \texttt{HPaC} and \texttt{RaC},
per coefficient family. Instances: \texttt{path-mixed}, all sizes pooled ($n$ up
to $2\,000$ for \texttt{PaC}, up to $100\,000$ for the others), 100 instances per
family for \texttt{HPaC} and \texttt{RaC}. Observations: the family changes
absolute times by $22\%$ for \texttt{RaC} ($21.2$--$25.8$\,ms) and $7\%$ for
\texttt{HPaC} ($2.9$--$3.1$\,ms),
with \texttt{near-ties} and \texttt{exact-ties} the fastest as everywhere else;
the paired \texttt{RaC}/\texttt{HPaC} ratio stays within $6.7$--$8.1$ across the
six families, so on paths too the family moves the amount of work and not the
ranking. Absolute times pool sizes and are therefore comparable only across
families, not against \cref{fig:path}.}{%
\tab{rep_times_campaign_d_path_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={CPU time (ms)}, ymode=log]
\addplot[pac]  table[x=idx, y=pac]  {\dfile{rep_plotfam_campaign_d_path}}; \addlegendentry{\texttt{PaC}}
\addplot[hpac] table[x=idx, y=hpac] {\dfile{rep_plotfam_campaign_d_path}}; \addlegendentry{\texttt{HPaC}}
\addplot[rac]  table[x=idx, y=rac]  {\dfile{rep_plotfam_campaign_d_path}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:binary}{%
Median CPU time in milliseconds of \texttt{PaC}, \texttt{HPaC}, \texttt{DHPaC}
and \texttt{RaC}, per size. Instances: \texttt{binary-mixed} (balanced binary
tree, random orientations), $n$ from $100$ to $100\,000$, 60 instances per size;
\texttt{PaC} up to $n=2\,000$. Observations: the median
\texttt{RaC}/\texttt{HPaC} ratio is between $3.2$ and $3.9$ over the whole
range -- lower than the $5.8$--$9.0$ of paths, since a balanced tree gives
\texttt{RaC}'s rake and compress rounds more to work with at each round, while
for the heap-based schedules a bounded degree keeps updates cheap on both
shapes.}{%
\tab{rep_times_campaign_d_binary}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, ymode=log, sizeaxis, ylabel={CPU time (ms)}]
\addplot[pac]  table[x=n, y=pac]  {\dat{campaign_d_binary}}; \addlegendentry{\texttt{PaC}}
\addplot[hpac] table[x=n, y=hpac] {\dat{campaign_d_binary}}; \addlegendentry{\texttt{HPaC}}
\addplot[dhpac] table[x=n, y=dhpac] {\dat{campaign_d_binary}}; \addlegendentry{\texttt{DHPaC}}
\addplot[rac]  table[x=n, y=rac]  {\dat{campaign_d_binary}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:binary_fam}{%
Median CPU time in milliseconds of \texttt{PaC}, \texttt{HPaC} and \texttt{RaC},
per coefficient family. Instances: \texttt{binary-mixed}, all sizes pooled ($n$
up to $2\,000$ for \texttt{PaC}, up to $100\,000$ for the others), 100 instances
per family for \texttt{HPaC} and \texttt{RaC}. Observations: the same flat
picture as on paths, with a narrower band -- $16.0$ to $18.9$\,ms for
\texttt{RaC} and $4.4$ to $4.7$\,ms for \texttt{HPaC}, i.e.\ $18\%$ and $7\%$ of
spread -- and the paired \texttt{RaC}/\texttt{HPaC} ratio between $3.4$ and $3.8$
across the six families. The two tie-stressing families are again the fastest,
and by the same relative amount for both algorithms, which is what ``the family
sets the amount of work'' means quantitatively.}{%
\tab{rep_times_campaign_d_binary_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={CPU time (ms)}, ymode=log]
\addplot[pac]  table[x=idx, y=pac]  {\dfile{rep_plotfam_campaign_d_binary}}; \addlegendentry{\texttt{PaC}}
\addplot[hpac] table[x=idx, y=hpac] {\dfile{rep_plotfam_campaign_d_binary}}; \addlegendentry{\texttt{HPaC}}
\addplot[rac]  table[x=idx, y=rac]  {\dfile{rep_plotfam_campaign_d_binary}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:star}{%
Median CPU time in milliseconds of \texttt{HPaC}, \texttt{DHPaC}, \texttt{PaC}
and \texttt{RaC}, per size. Instances: \texttt{star-mixed} (one hub, $n-1$
leaves, random orientations), $n$ from $100$ to $100\,000$, 60 instances per
size; the heap-based algorithms up to $n=20\,000$, \texttt{PaC} and \texttt{RaC}
over the full range. Observations: this is the class that inverts the ordering.
\texttt{RaC} is already faster at $n=100$ and $\approx250\times$
faster at $n=20\,000$. Every peel or contraction touches the hub and
invalidates the candidate ratios of a constant fraction of the remaining
leaves, so \emph{any} heap-based schedule pays $\Theta(n)$ maintenance per
event, while \texttt{RaC}'s rake operations absorb all leaves in
$\mathcal O(\log n)$ rounds regardless of the hub's update history.
\texttt{PaC}, which maintains no candidate structure at all, stays an order of
magnitude below \texttt{HPaC} -- on this class the schedule that maintains
nothing beats the one that maintains a heap.}{%
\tab{rep_times_campaign_d_star}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, ymode=log, sizeaxis, ylabel={CPU time (ms)}]
\addplot[hpac] table[x=n, y=hpac] {\dat{campaign_d_star}}; \addlegendentry{\texttt{HPaC}}
\addplot[dhpac] table[x=n, y=dhpac] {\dat{campaign_d_star}}; \addlegendentry{\texttt{DHPaC}}
\addplot[pac]  table[x=n, y=pac]  {\dat{campaign_d_star}}; \addlegendentry{\texttt{PaC}}
\addplot[rac]  table[x=n, y=rac]  {\dat{campaign_d_star}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:star_fam}{%
Median CPU time in milliseconds of \texttt{PaC}, \texttt{HPaC} and \texttt{RaC},
per coefficient family. Instances: \texttt{star-mixed}, all sizes pooled
($n\le20\,000$ for \texttt{HPaC}, up to $100\,000$ for \texttt{PaC} and
\texttt{RaC}), 100 instances per family. Observations: the inversion of
\cref{fig:star} is present in every family and its size barely moves -- the
paired \texttt{RaC}/\texttt{HPaC} ratio lies between $0.060$ and $0.064$ in all
six -- so the inversion is purely topological: no coefficient family produces it
and none removes it. Note the vertical scale: \texttt{HPaC} is two orders of
magnitude above \texttt{RaC} on this class, whereas on every other class it is
below.}{%
\tab{rep_times_campaign_d_star_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={CPU time (ms)}, ymode=log]
\addplot[pac]  table[x=idx, y=pac]  {\dfile{rep_plotfam_campaign_d_star}}; \addlegendentry{\texttt{PaC}}
\addplot[hpac] table[x=idx, y=hpac] {\dfile{rep_plotfam_campaign_d_star}}; \addlegendentry{\texttt{HPaC}}
\addplot[rac]  table[x=idx, y=rac]  {\dfile{rep_plotfam_campaign_d_star}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:star_racpac}{%
Median of the paired per-instance ratio \texttt{RaC}/\texttt{PaC}, per size, with
the number of paired instances and the IQR. Instances: \texttt{star-mixed}, $n$
from $100$ to $100\,000$, 60 instances per size. Observations: this pairs the two
algorithms that are \emph{not} penalized by a candidate structure on a hub, and
therefore isolates the asymptotic difference $\mathcal O(n\log n)$ against
$\mathcal O(n^2)$ on the shape where it is visible. The crossover sits between
$n=1\,000$ and $n=2\,000$ ($1.15$ then $0.52$); at $n=10^5$ the ratio is $0.007$,
i.e.\ \texttt{RaC} is $135\times$ faster, and the IQR is below $0.001$, so the
effect is uniform over instances and not driven by outliers. Dashed line marks
parity.}{%
\tab{rep_ratio_campaign_d_star_rac_over_pac_by_size}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, ymode=log, sizeaxis, ylabel={\texttt{RaC}/\texttt{PaC}},
  extra y ticks={1}, extra y tick style={grid=major, grid style={black!45, dashed}}]
\addplot[rac] table[x=n, y expr=\thisrow{rac}/\thisrow{pac}] {\dat{campaign_d_star}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\subsection{What the structured classes say}

\begin{itemize}
  \item \textbf{Paths and binary trees add no new regime.} The ratios sit
  where the random forests put them ($5.8$--$9.0$ and $3.2$--$3.9$) and are
  flat in $n$ over three orders of magnitude: on these shapes the asymptotic
  advantage of \texttt{RaC} never materializes within the sizes we can run.
  \item \textbf{Stars are the counterexample the theory predicts, and they
  are extreme.} The ratio moves by four orders of magnitude across the size
  range, from $0.75$ to $0.004$. This is the one place in the study where the
  complexity separation is not academic.
  \item \textbf{The star penalty is a property of the \emph{schedule}, not of
  the algorithm.} \texttt{PaC} and \texttt{HPaC} implement the same
  algorithm; on stars the heap-based schedule is an order of magnitude slower
  than the scan-based one (\cref{fig:star}), because maintaining candidate
  ratios is exactly what a hub makes expensive. A practitioner facing
  hub-shaped precedence graphs should reach for \texttt{RaC}, and failing that
  for the direct scan -- not for the heap.
\end{itemize}

\section{Single orientations}\label{sec:oriented}

When the forest is known in advance to be a forest of in-trees or of
out-trees, a specialized algorithm can exploit it. This section measures how
much that knowledge is worth: it is a constant factor, not a change of regime,
and the constant is remarkably stable across sizes, densities and coefficient
families.

\tabfig{fig:ein}{%
Median CPU time in milliseconds of \texttt{HPaC}, \texttt{DHPaC}, the
specialized \texttt{HIPaC} and \texttt{RaC}, per size. Instances:
\texttt{in-forest} (out-degree $\le1$), $n\in\{100,\dots,1\,000\}\cup\{10\,000,
\dots,100\,000\}$, 240 instances per size. Observations: \texttt{HIPaC} is below
\texttt{HPaC} at every size and the two curves stay parallel, so the
specialization is a constant factor and not a change of regime. The mechanism:
on a single orientation the minimal preceding set of every arc collapses to a
singleton, so one heap value per vertex suffices and no closure sum is ever
propagated.}{%
\tab{rep_times_campaign_e_in}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, ymode=log, sizeaxis, ylabel={CPU time (ms)}]
\addplot[hpac]  table[x=n, y=hpac]  {\dat{campaign_e_in}}; \addlegendentry{\texttt{HPaC}}
\addplot[dhpac] table[x=n, y=dhpac] {\dat{campaign_e_in}}; \addlegendentry{\texttt{DHPaC}}
\addplot[hipac] table[x=n, y=hipac] {\dat{campaign_e_in}}; \addlegendentry{\texttt{HIPaC}}
\addplot[rac]   table[x=n, y=rac]   {\dat{campaign_e_in}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:ein_ratio}{%
Median of the paired per-instance ratio \texttt{HIPaC}/\texttt{HPaC}, per size,
with the number of paired instances and the IQR. Instances: \texttt{in-forest},
$n$ from $100$ to $100\,000$, 240 instances per size (all four densities and six
families). Observations: the ratio lies between $0.52$ and $0.75$ with no erosion
up to $n=10^5$ -- if anything the advantage grows mildly with $n$. The IQR is
large ($0.44$ to $0.82$) because these 240 instances mix four densities, on which
the gain differs by a factor 6: \cref{fig:e_rho} separates them. Dashed line
marks parity.}{%
\tab{rep_ratio_campaign_e_in_hipac_over_hpac_by_size}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, pcfplot, xlabel={$n$}, ylabel={\texttt{HIPaC}/\texttt{HPaC}},
  ymin=0, ymax=1.1,
  extra y ticks={1}, extra y tick style={grid=major, grid style={black!45, dashed}}]
\addplot[hipac] table[x=n, y expr=\thisrow{hipac}/\thisrow{hpac}] {\dat{campaign_e_in}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:eout}{%
Median CPU time in milliseconds of \texttt{HPaC}, \texttt{DHPaC}, the
specialized \texttt{HOPaC} and \texttt{RaC}, per size. Instances:
\texttt{out-forest} (in-degree $\le1$), $n$ from $100$ to $100\,000$, 240
instances per size. Observations: the picture mirrors the in-forest one of
\cref{fig:ein} at every size, as the symmetry of the two constructions predicts;
the dual algorithm on the dual class buys the same constant factor, which is also
a consistency check on the two implementations.}{%
\tab{rep_times_campaign_e_out}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, ymode=log, sizeaxis, ylabel={CPU time (ms)}]
\addplot[hpac]  table[x=n, y=hpac]  {\dat{campaign_e_out}}; \addlegendentry{\texttt{HPaC}}
\addplot[dhpac] table[x=n, y=dhpac] {\dat{campaign_e_out}}; \addlegendentry{\texttt{DHPaC}}
\addplot[hopac] table[x=n, y=hopac] {\dat{campaign_e_out}}; \addlegendentry{\texttt{HOPaC}}
\addplot[rac]   table[x=n, y=rac]   {\dat{campaign_e_out}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:eout_ratio}{%
Median of the paired per-instance ratio \texttt{HOPaC}/\texttt{HPaC}, per size,
with the number of paired instances and the IQR. Instances: \texttt{out-forest},
$n$ from $100$ to $100\,000$, 240 instances per size. Observations: the ratio
lies between $0.57$ and $0.78$ and is never more than $0.09$ from the in-forest
value at the same size (\cref{fig:ein_ratio}) -- the two specializations are worth
the same constant, and neither erodes with $n$. Dashed line marks parity.}{%
\tab{rep_ratio_campaign_e_out_hopac_over_hpac_by_size}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, pcfplot, xlabel={$n$}, ylabel={\texttt{HOPaC}/\texttt{HPaC}},
  ymin=0, ymax=1.1,
  extra y ticks={1}, extra y tick style={grid=major, grid style={black!45, dashed}}]
\addplot[hopac] table[x=n, y expr=\thisrow{hopac}/\thisrow{hpac}] {\dat{campaign_e_out}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:e_rho}{%
Median CPU time in milliseconds of \texttt{HPaC}, the specialized \texttt{HIPaC}
and \texttt{RaC}, per arc density. Instances: \texttt{in-forest}, all twenty
sizes pooled, 1\,200 instances per density. Observations: this is where the
specialization's gain actually comes from, and it is \emph{not} uniform. From
$\varrho=0.3$ to $\varrho=1.0$, \texttt{HPaC} slows down by a factor 9 ($1.9$ to
$17.2$\,ms) while \texttt{HIPaC} slows down by less than a factor 2 ($2.1$ to
$3.9$\,ms): the paired ratio \texttt{HIPaC}/\texttt{HPaC} therefore falls from
$1.23$ at $\varrho=0.3$ -- where the specialization is a small \emph{loss} -- to
$0.84$, $0.34$ and $0.21$ at $\varrho=0.6$, $0.9$ and $1.0$, a $4.7\times$ gain on
single spanning in-trees. The reason is exactly the propagation the
specialization removes: on a fragmented forest of mostly isolated vertices
(\cref{fig:forest}) there is almost no closure sum to propagate, so
\texttt{HPaC} pays nothing for its generality and \texttt{HIPaC}'s setup cost is
not recovered; the more connected the forest, the more the general algorithm
propagates. The single figure ``$0.52$--$0.75$'' of \cref{fig:ein_ratio} is thus
an average over four quite different regimes.}{%
\tab{rep_times_campaign_e_in_by_rho}}{%
\begin{tikzpicture}
\begin{axis}[rhoaxis, ylabel={CPU time (ms)}, ymode=log]
\addplot[hpac]  table[x=rho, y=hpac]  {\dfile{rep_plotrho_campaign_e_in}}; \addlegendentry{\texttt{HPaC}}
\addplot[hipac] table[x=rho, y=hipac] {\dfile{rep_plotrho_campaign_e_in}}; \addlegendentry{\texttt{HIPaC}}
\addplot[rac]   table[x=rho, y=rac]   {\dfile{rep_plotrho_campaign_e_in}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:e_ratio_rho}{%
Median of the paired per-instance ratio \texttt{HIPaC}/\texttt{HPaC}, per arc
density, with the number of paired instances and the IQR. Instances:
\texttt{in-forest}, all twenty sizes pooled, 1\,200 instances per density.
Observations: the same effect as \cref{fig:e_rho} in paired form, which is the
form that admits no aggregation artefact -- the ratio is computed on each
instance and then aggregated. It crosses parity between $\varrho=0.3$ and
$\varrho=0.6$, and the IQR stays below $0.17$ throughout, so within one density
the effect is tight: the wide IQR of \cref{fig:ein_ratio} is entirely the mixture
of densities. Practical reading: the specialized variants are worth having when
the input is a connected in-tree or out-tree, and worth nothing on a heavily
fragmented forest.}{%
\tab{rep_ratio_campaign_e_in_hipac_over_hpac_by_rho}}{%
\begin{tikzpicture}
\begin{axis}[rhoaxis, ylabel={\texttt{HIPaC}/\texttt{HPaC}}, ymin=0, ymax=1.4,
  extra y ticks={1}, extra y tick style={grid=major, grid style={black!45, dashed}}]
\addplot[hipac] table[x=rho, y=ratio] {\dfile{rep_plotratiorho_campaign_e_in_hipac_hpac}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:e_fam}{%
Median of the paired per-instance ratio \texttt{HIPaC}/\texttt{HPaC}, per
coefficient family, with the number of paired instances and the IQR. Instances:
\texttt{in-forest}, all sizes and densities pooled, 800 instances per family.
Observations: the ratio lies between $0.65$ and $0.70$ in all six families, a
$7\%$ spread -- against the factor 6 across densities of \cref{fig:e_ratio_rho}.
There is no family in which the advantage of the specialization disappears, and
none in which it is much larger: as everywhere else in this report, the
coefficients set the amount of work and the topology sets who wins.}{%
\tab{rep_ratio_campaign_e_in_hipac_over_hpac_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={\texttt{HIPaC}/\texttt{HPaC}}, ymin=0, ymax=1]
\addplot[hipac] table[x=idx, y=ratio] {\dfile{rep_plotratiofam_campaign_e_in_hipac_hpac}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:e_rac}{%
Median of the paired per-instance ratio \texttt{RaC}/\texttt{HPaC}, per
coefficient family, with the number of paired instances and the IQR. Instances:
\texttt{in-forest}, all sizes and densities pooled, 800 instances per family
(out-forests give the same values within $6\%$). Observations: the ratio is
$3.0$--$3.2$ in every family, i.e.\ flat as on mixed forests, while by size it
grows from about $2.8\times$ at $n\le1\,000$ to $12.8\times$ at $n=10^5$ -- so
restricting the orientation does not change \texttt{RaC}'s standing against the
heap-based algorithms, it only removes the need for closure-sum propagation from
the latter.}{%
\tab{rep_ratio_campaign_e_in_rac_over_hpac_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={\texttt{RaC}/\texttt{HPaC}}, ymin=0]
\addplot[rac] table[x=idx, y=ratio] {\dfile{rep_plotratiofam_campaign_e_in_rac_hpac}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\paragraph{Note on the comparison with the first version of the manuscript.}
The specialized variants were reported there as $4$--$10\times$ faster than
the general algorithm, against $1.3$--$1.9\times$ here. The direction is
expected -- the general \texttt{HPaC} is substantially faster than it was, see
\cref{sec:heap} -- but the magnitude is only partly accounted for by the heap
policy: comparing the two heap implementations directly on these instances
gives $1.3$--$1.4\times$, not $3$--$4\times$. The remainder is attributable to
the code being an independent rewrite rather than a refactoring, and to
instances regenerated under different coefficient families. Absolute times
across the two versions are therefore not comparable; only trends are.

\subsection{What the oriented classes say}

\begin{itemize}
  \item \textbf{The specialization buys a constant, around $1.5\times$ to
  $2\times$ overall, and it does not erode with size.} The ratio at $n=10^5$ is
  as good as at $n=10^3$ (\cref{fig:ein_ratio,fig:eout_ratio}) and it is the same
  in all six coefficient families (\cref{fig:e_fam}).
  \item \textbf{But that constant is a strong function of the density, and this
  is the one place where the aggregate figure hides the effect.} The gain runs
  from $1.23$ (a small loss) on forests with $\varrho=0.3$ to $0.21$ (a
  $4.7\times$ gain) on single spanning in-trees (\cref{fig:e_rho,fig:e_ratio_rho}).
  What the specialization removes is closure-sum propagation, and there is
  nothing to propagate in a forest of mostly isolated vertices: the more
  connected the input, the more the specialization is worth. Reported as a single
  number, the specialization looks like a modest constant; reported per density,
  it ranges from useless to a factor five.
  \item \textbf{It is smaller than the theory might suggest.} Both
  \texttt{HIPaC} and the general \texttt{HPaC} behave like
  $\mathcal O(n\log n)$ in practice on these instances, so what separates them
  is bookkeeping, not asymptotics: one heap value per vertex instead of two
  heaps plus closure-sum propagation.
  \item \textbf{Knowing the orientation is worth less than knowing the
  shape.} A factor $1.5$--$2$ for a single orientation, against a factor $250$
  for a star. If one piece of structural information about the input can be
  exploited, the shape is the one that pays.
\end{itemize}

\section{Implementation note: heap policy}\label{sec:heap}

The candidate heaps admit two implementations, indistinguishable in the
asymptotic time bound and very different in practice.

\begin{itemize}
  \item \textbf{Lazy deletion (push-only).} Every closure-sum change pushes a
  fresh entry; stale ones are dropped only when they surface at the top. Heap
  size is bounded by the number of touch operations, not by $n$. On a star this
  is pathological: every event touches the hub, each touch re-pushes one entry
  per incident arc, so the heap accumulates $\Theta(n^2)$ entries.
  \item \textbf{Periodic rebuild (used throughout this study).} Each heap is
  rebuilt from the live candidates whenever its size exceeds a constant
  multiple of the live candidate count: amortized $\mathcal O(\log n)$ per
  operation, worst-case time bound unchanged, memory $\mathcal O(n)$ on
  \emph{every} input. This is what makes the implementation attain the space
  bound proved in the manuscript.
\end{itemize}

\tabfig{fig:heap}{%
CPU time in milliseconds and peak RSS in MiB of \texttt{HPaC} under the two heap
policies, per size; the plot shows the memory column of the table. Instances:
\texttt{star-mixed}, \texttt{independent-positive}, seed 0, $n$ from $1\,000$ to
$10\,000$ -- one instance per size, one repetition, each algorithm invoked in its
own process, which is the only regime in which peak RSS is attributable to a
single algorithm (\cref{sec:setup}). Observations: the lazy heap's memory grows by
a factor of 100 when $n$ grows by 10 -- quadratic, as predicted -- reaching
$1.5$\,GiB at $n=10^4$ where the rebuild policy uses $8$\,MiB; extrapolating, the
lazy policy would exhaust the 8\,GiB ceiling by $n\approx20\,000$. The rebuild
policy is also $\approx4\times$ faster here (and $\approx2\times$ on large random
forests), with bit-identical output on every instance tested, so the two policies
are not a trade-off: one dominates. For reference, \texttt{PaC}, which maintains
no candidate structure, peaks at $5.6$\,MiB at $n=10^4$. The practical lesson: on
this problem the choice of data structure inside one algorithm is worth as much
as the choice between algorithms.}{%
\begin{tabular}{@{}rrrrrr@{}}
\toprule
& & \multicolumn{2}{c}{lazy deletion} & \multicolumn{2}{c}{periodic rebuild} \\
\cmidrule(lr){3-4}\cmidrule(l){5-6}
$n$ & \#inst & ms & MiB & ms & MiB \\
\midrule
1\,000 & 1 & 139.0 & 15.6 & 64.1 & 3.9 \\
2\,000 & 1 & 628.7 & 51.5 & 233.1 & 4.0 \\
5\,000 & 1 & 5\,440.9 & 388.0 & 1\,422.1 & 6.4 \\
10\,000 & 1 & 27\,020.7 & 1\,541.2 & 6\,396.5 & 8.2 \\
\bottomrule
\end{tabular}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, ymode=log, sizeaxis, ylabel={peak RSS (MiB)}]
\addplot[q2] table[x=n, y=rss_lazy]    {\dfile{rep_plotheap}}; \addlegendentry{lazy deletion}
\addplot[q1] table[x=n, y=rss_bounded] {\dfile{rep_plotheap}}; \addlegendentry{periodic rebuild}
\end{axis}
\end{tikzpicture}}

\section{Comparison with parametric pseudoflow}\label{sec:bppf}

\texttt{BPPF} evaluates minimum cuts at user-supplied parameter values, so the
comparison must decide what to supply. We drive it in the setting most
favourable to it, identical to the methodology of the manuscript's first
version: one \texttt{BPPF} process per instance receives, in \texttt{BPPF}'s
native affine capacity format, the $k+1$ values that bracket all $k$
breakpoints -- so it is spared the search for the breakpoints, and one call
certifies the whole parametric solution. Timing is \texttt{BPPF}'s own
internal solve timer (input parsing excluded) against \texttt{HPaC}'s
in-process time on the same instance; fixed-point precision $10^{-6}$; all
2\,400 instances with $n\le1\,000$.

\tabfig{fig:bppf}{%
Median CPU time in milliseconds of \texttt{HPaC} and of \texttt{BPPF}, per size:
\texttt{HPaC}'s in-process time for the full parametric sweep against
\texttt{BPPF}'s own internal solve timer, input parsing excluded. Instances:
\texttt{mixed-forest}, $n\in\{100,200,\dots,1\,000\}$, 240 instances per size,
2\,400 in total; \texttt{BPPF} is given the $k+1$ parameter values bracketing the
$k$ breakpoints, in its native affine capacity format, precision $10^{-6}$.
Observations: the median paired ratio \texttt{BPPF}/\texttt{HPaC} grows from
$1.5$ at $n=100$ to $4.5$ at $n=1\,000$, overall median $3.3$, even though the
breakpoints are handed to \texttt{BPPF} for free. Scope: forests with
$n\le1\,000$, whereas \texttt{BPPF} solves a strictly more general problem on
arbitrary precedence graphs, so nothing here transfers outside this class -- and
recall that on a forest \texttt{BPPF}'s bound reads $\mathcal O(n^2\log n)$
(\cref{tab:algos}), so the measured factor of a few is far kinder to it than the
bounds. The times are sub-millisecond at the small end, which is why the ratio is
computed per instance and not from these two columns.}{%
\begin{tabular}{@{}rrrr@{}}
\toprule
& & \multicolumn{2}{c}{CPU time (ms)} \\
\cmidrule(l){3-4}
$n$ & \#inst & \texttt{HPaC} & \texttt{BPPF} \\
\midrule
100 & 240 & 0.1 & 0.1 \\
200 & 240 & 0.1 & 0.3 \\
300 & 240 & 0.2 & 0.6 \\
400 & 240 & 0.3 & 1.0 \\
500 & 240 & 0.4 & 1.5 \\
600 & 240 & 0.5 & 2.1 \\
700 & 240 & 0.5 & 2.8 \\
800 & 240 & 0.7 & 3.6 \\
900 & 240 & 0.8 & 4.5 \\
1\,000 & 240 & 0.9 & 5.5 \\
\bottomrule
\end{tabular}}{%
\begin{tikzpicture}
\begin{axis}[ymode=log, pcfplot, xlabel={$n$}, ylabel={CPU time (ms)},
  xtick={100,400,700,1000}, scaled x ticks=false]
\addplot[bppf] coordinates {(100,0.105) (200,0.305) (300,0.604) (400,0.991)
  (500,1.509) (600,2.086) (700,2.777) (800,3.581) (900,4.507) (1000,5.526)};
\addlegendentry{\texttt{BPPF}}
\addplot[hpac] coordinates {(100,0.061) (200,0.137) (300,0.221) (400,0.294)
  (500,0.382) (600,0.460) (700,0.530) (800,0.658) (900,0.752) (1000,0.871)};
\addlegendentry{\texttt{HPaC}}
\end{axis}
\end{tikzpicture}}

\paragraph{Agreement and precision.} The closures returned by the two methods
agree at every probe on every instance, with one exception class: on
\textbf{16 of the 2\,400 instances} \texttt{BPPF} merges two consecutive
closure layers whose thresholds differ by less than its fixed-point precision,
and therefore does not recover the exact optimal layer sequence. All 16 belong
to \texttt{near-ties} and \texttt{exact-ties}, the two families designed to
place thresholds at exactly that distance -- which is also why those families
exist. Our algorithms are unaffected, comparing integers exactly.

\paragraph{Discarded variant.} An earlier version of this comparison drove
\texttt{BPPF} once per breakpoint, one process spawn per call. It is reported
nowhere: the resulting ratio is dominated by process-creation overhead and
says nothing about either algorithm.

\section{Dispersion of the measurements}\label{sec:dispersion}

Every ratio reported above is a median over instances of a median over
repetitions. This section reports the second-level dispersion -- how much the
repetitions of a single instance differ from each other -- because that is the
noise floor against which every claimed difference has to be read. It is
expressed as the relative IQR (\cref{sec:defs}): the IQR over the repetitions
of one instance, divided by that instance's median time.

\tabfig{fig:iqr_c}{%
Median relative IQR of the timing, in percent, per size: for each instance, the
IQR over its repetitions divided by its own median time, then the median of that
over instances. Instances: \texttt{mixed-forest},
$n\in\{10\,000,\dots,100\,000\}$, 240 instances per size, 3 repetitions each.
Observations: $0.5\%$ to $1.8\%$ for \texttt{HPaC}, $1.6\%$ to $2.3\%$ for
\texttt{DHPaC}, and $4.6\%$ falling to $1.2\%$ for \texttt{RaC} -- the longer a
run, the smaller its relative noise, which is why the largest sizes are the
quietest. This is the number that
says how much of a reported difference is signal: every ratio in this report is at
least an order of magnitude larger than the measurement noise it rests on. The
single exception is the near-tie $0.91$ against $1$ at $n=100$ in
\cref{sec:bcamp}, which is for that reason reported as a near-tie and not as an
advantage.}{%
\tab{rep_iqr_campaign_c}}{%
\begin{tikzpicture}
\begin{axis}[pcfplot, xlabel={$n$}, ylabel={relative IQR (\%)},
  xtick={10000,40000,70000,100000}, scaled x ticks=false, ymin=0,
  /pgf/number format/.cd, fixed, 1000 sep={}, /tikz/.cd]
\addplot[rac]   table[x=n, y=rac]   {\dfile{rep_plotiqr_campaign_c}}; \addlegendentry{\texttt{RaC}}
\addplot[dhpac] table[x=n, y=dhpac] {\dfile{rep_plotiqr_campaign_c}}; \addlegendentry{\texttt{DHPaC}}
\addplot[hpac]  table[x=n, y=hpac]  {\dfile{rep_plotiqr_campaign_c}}; \addlegendentry{\texttt{HPaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:iqr_e}{%
Median relative IQR of the timing, in percent, per size, defined as in
\cref{fig:iqr_c}. Instances: \texttt{in-forest}, $n$ from $100$ to $100\,000$,
240 instances per size, 3 repetitions each. Observations: $2.2\%$--$4.2\%$ for
\texttt{HIPaC} against $2.5\%$--$8.7\%$ for the general \texttt{HPaC}, so the
specialized variant is at least as stable as the algorithm it replaces and its
advantage is not an artefact of variance; \texttt{RaC} goes from $12.3\%$ at
$n=100$ down to $1.1\%$ at $n=10^5$. Noise is largest at $n=100$, where a run
lasts tens of microseconds and the timer resolution starts to show; from $n=200$
on every algorithm stays below $6\%$, an order of magnitude below the
$1.5$--$4.7\times$ effects measured in \cref{sec:oriented}.}{%
\tab{rep_iqr_campaign_e_in}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, pcfplot, xlabel={$n$}, ylabel={relative IQR (\%)}, ymin=0]
\addplot[rac]   table[x=n, y=rac]   {\dfile{rep_plotiqr_campaign_e_in}}; \addlegendentry{\texttt{RaC}}
\addplot[hipac] table[x=n, y=hipac] {\dfile{rep_plotiqr_campaign_e_in}}; \addlegendentry{\texttt{HIPaC}}
\addplot[hpac]  table[x=n, y=hpac]  {\dfile{rep_plotiqr_campaign_e_in}}; \addlegendentry{\texttt{HPaC}}
\end{axis}
\end{tikzpicture}}

\appendix

\section{Per-family detail at every size}\label{app:detail}

The full cross-tabulation behind every by-family statement above: median CPU
time per coefficient family and per size, for the two headline algorithms, on
both random campaigns. The family ordering is stable across sizes -- the two
tie-stressing families are consistently the fastest for both algorithms, by a
margin that widens with $n$ (at $n=10^5$, \texttt{near-ties} costs
\texttt{HPaC} about $12\%$ less than \texttt{independent-positive}) -- and no
family ever reverses the relative standing of the two algorithms. The plots
show four representative families to stay readable; the tables give all six.

\tabfig{fig:detail_b_hpac}{%
Median CPU time of \texttt{HPaC} in milliseconds, one column per coefficient
family and one row per size. Instances: \texttt{mixed-forest},
$n\in\{100,\dots,1\,000\}$, all four densities, 40 instances per cell. The plot
shows four of the six families, the table all six. Observations: at these sizes
all times are sub-millisecond and the six families are indistinguishable at the
reported resolution -- the largest gap at any size is one tenth of a
millisecond, i.e.\ the last digit. The family effect exists but needs
$n\ge10^4$ to be measurable (\cref{fig:detail_c_hpac}); this table is here to
show that it is \emph{not} measurable below that, so no conclusion in
\cref{sec:bcamp} rests on it.}{%
\tab{rep_detail_campaign_b_hpac}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, ymode=log, sizeaxis, ylabel={\texttt{HPaC} time (ms)}, legend columns=2]
\addplot[hpac] table[x=n, y=independentpositive] {\dfile{rep_plotdetail_campaign_b_hpac}};
\addlegendentry{ind-pos}
\addplot[hpac, dashed, mark=o] table[x=n, y=correlated] {\dfile{rep_plotdetail_campaign_b_hpac}};
\addlegendentry{corr}
\addplot[aux] table[x=n, y=nearties] {\dfile{rep_plotdetail_campaign_b_hpac}};
\addlegendentry{near-ties}
\addplot[aux2] table[x=n, y=exactties] {\dfile{rep_plotdetail_campaign_b_hpac}};
\addlegendentry{exact-ties}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:detail_b_rac}{%
Median CPU time of \texttt{RaC} in milliseconds, one column per coefficient
family and one row per size. Instances: \texttt{mixed-forest},
$n\in\{100,\dots,1\,000\}$, all four densities, 40 instances per cell. The plot
shows four of the six families, the table all six. Observations: the same picture
as \cref{fig:detail_b_hpac} a factor of three higher, with the families within
$0.5$\,ms of each other at $n=1\,000$. Every family shows the same dip from
$n=900$ to $n=1\,000$ ($3.5$ to $3.1$\,ms for \texttt{anti-correlated}), and so
does \texttt{HPaC}, while the number of closure layers keeps growing ($470$ to
$528$) and \texttt{PaC} keeps growing with it. We attribute the two elevated
sizes to memory-bandwidth contention between the parallel lanes of the sweep
(\cref{sec:setup}), which touches the two memory-bound algorithms and not the
scan-based one; the effect is $\approx10\%$, an order of magnitude below every
ratio reported in this document, and it is visible only in this per-cell
breakdown.}{%
\tab{rep_detail_campaign_b_rac}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, ymode=log, sizeaxis, ylabel={\texttt{RaC} time (ms)}, legend columns=2]
\addplot[rac] table[x=n, y=independentpositive] {\dfile{rep_plotdetail_campaign_b_rac}};
\addlegendentry{ind-pos}
\addplot[rac, dashed, mark=o] table[x=n, y=correlated] {\dfile{rep_plotdetail_campaign_b_rac}};
\addlegendentry{corr}
\addplot[aux] table[x=n, y=nearties] {\dfile{rep_plotdetail_campaign_b_rac}};
\addlegendentry{near-ties}
\addplot[aux2] table[x=n, y=exactties] {\dfile{rep_plotdetail_campaign_b_rac}};
\addlegendentry{exact-ties}
\end{axis}
\end{tikzpicture}}

\tabfigwide{fig:detail_c_hpac}{%
Median CPU time of \texttt{HPaC} in milliseconds, one column per coefficient
family and one row per size. Instances: \texttt{mixed-forest},
$n\in\{10\,000,\dots,100\,000\}$, all four densities, 40 instances per cell. The
plot shows four of the six families, the table all six. Observations: here the
family effect is measurable and consistent -- the two tie-stressing families
(\texttt{near-ties}, \texttt{exact-ties}) occupy the two fastest columns at 9 of
the 10 sizes, and \texttt{anti-correlated} or \texttt{independent-signed} the
slowest -- with a spread between fastest and slowest family of $8$--$17\%$ that
neither grows nor shrinks with $n$. Compare \cref{fig:layersfam}:
\texttt{near-ties} produces $27\times$ fewer
closure layers than \texttt{independent-signed} at these sizes, yet costs only
about $10\%$ less time -- so the per-layer cost is far from constant, and most of
\texttt{HPaC}'s work at $n=10^5$ is proportional to $n$ (building and rebuilding
the heaps) rather than to the number of breakpoints.}{%
\tab{rep_detail_campaign_c_hpac}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, ymode=log, sizeaxis, ylabel={\texttt{HPaC} time (ms)}, legend columns=2]
\addplot[hpac] table[x=n, y=independentpositive] {\dfile{rep_plotdetail_campaign_c_hpac}};
\addlegendentry{ind-pos}
\addplot[hpac, dashed, mark=o] table[x=n, y=correlated] {\dfile{rep_plotdetail_campaign_c_hpac}};
\addlegendentry{corr}
\addplot[aux] table[x=n, y=nearties] {\dfile{rep_plotdetail_campaign_c_hpac}};
\addlegendentry{near-ties}
\addplot[aux2] table[x=n, y=exactties] {\dfile{rep_plotdetail_campaign_c_hpac}};
\addlegendentry{exact-ties}
\end{axis}
\end{tikzpicture}}

\tabfigwide{fig:detail_c_rac}{%
Median CPU time of \texttt{RaC} in milliseconds, one column per coefficient
family and one row per size. Instances: \texttt{mixed-forest},
$n\in\{10\,000,\dots,100\,000\}$, all four densities, 40 instances per cell. The
plot shows four of the six families, the table all six. Observations: the family
spread is $5$--$11\%$, comparable to \texttt{HPaC}'s, and the ordering is
the same, which is why the paired ratio of \cref{fig:c_ratio_fam} is flat: the
two algorithms react to the coefficients in the same way. \texttt{RaC}'s growth
over this range is the steepest of any algorithm here -- a factor $33$ ($53$ to
$1\,766$\,ms) for a $10\times$ increase in $n$, against a factor $17$ for
\texttt{HPaC} -- which is consistent with its being the only algorithm whose
space is $\mathcal O(n\log n)$: past a few tens of thousands of vertices its
top-tree structures no longer fit in cache and every contraction round pays
main-memory latency. We report this as an interpretation, not as a measurement:
we did not instrument cache misses.}{%
\tab{rep_detail_campaign_c_rac}}{%
\begin{tikzpicture}
\begin{axis}[xmode=log, ymode=log, sizeaxis, ylabel={\texttt{RaC} time (ms)}, legend columns=2]
\addplot[rac] table[x=n, y=independentpositive] {\dfile{rep_plotdetail_campaign_c_rac}};
\addlegendentry{ind-pos}
\addplot[rac, dashed, mark=o] table[x=n, y=correlated] {\dfile{rep_plotdetail_campaign_c_rac}};
\addlegendentry{corr}
\addplot[aux] table[x=n, y=nearties] {\dfile{rep_plotdetail_campaign_c_rac}};
\addlegendentry{near-ties}
\addplot[aux2] table[x=n, y=exactties] {\dfile{rep_plotdetail_campaign_c_rac}};
\addlegendentry{exact-ties}
\end{axis}
\end{tikzpicture}}

\section{Reproducing this report}

\begin{verbatim}
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build -j
ctest --test-dir build --output-on-failure   # correctness gate
tools/run_night_sweep.sh                     # all campaigns, one command
tools/build_report.sh                        # aggregate + emit all tables
cd report && latexmk -pdf computational_report.tex
\end{verbatim}

\noindent
Code, generators, raw and processed data:
\sloppy\url{https://github.com/fabiofurini/parametric-closure-forests}, release
\texttt{v0.3.0} (instance archives split under GitHub's asset size limit, with
SHA-256 checksums). The pseudoflow baseline is the unmodified upstream
implementation at
\url{https://github.com/hochbaumGroup/Bounded-precision-simple-parametric.git}.

\end{document}

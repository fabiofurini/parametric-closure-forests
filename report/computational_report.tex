\documentclass[11pt]{article}

\usepackage[margin=2.3cm]{geometry}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{array}
\usepackage{graphicx}
\usepackage{adjustbox}
\usepackage{xcolor}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}
\usepackage[capitalise]{cleveref}
\usepackage{url}
\usepackage{float}
\usepackage{enumitem}
\setlist{nosep,leftmargin=1.4em}

\definecolor{darkgreen}{rgb}{0.0, 0.5, 0.0}

% One shared axis style; legend outside the axis so it never covers a curve.
\pgfplotsset{pcfplot/.style={
  width=0.97\linewidth, height=5.1cm,
  tick label style={font=\scriptsize}, label style={font=\small},
  legend style={font=\scriptsize, at={(0.5,1.03)}, anchor=south,
                legend columns=3, draw=none, fill=none,
                /tikz/every even column/.append style={column sep=6pt}},
  grid=major, grid style={gray!25}, /pgfplots/.cd}}

% One colour / marker / dash per algorithm, identical in every plot of this
% report and of the manuscript.
\pgfplotsset{
  pac/.style   ={color=red!80,          mark=square*,   thick},
  dpac/.style  ={color=red!80,          mark=diamond*,  thick, dashed},
  hpac/.style  ={color=darkgreen,       mark=*,         thick},
  dhpac/.style ={color=darkgreen,       mark=pentagon*, thick, dashed},
  rac/.style   ={color=blue!70,         mark=triangle*, thick},
  hipac/.style ={color=orange!90!black, mark=triangle*, thick},
  hopac/.style ={color=violet,          mark=square*,   thick},
  bppf/.style  ={color=brown,           mark=square*,   thick},
  aux/.style   ={color=black!55,        mark=o,         thick},
  aux2/.style  ={color=black!35,        mark=x,         thick, dashed}}

\pgfplotsset{famaxis/.style={xmin=0.5, xmax=6.5,
  xtick={1,2,3,4,5,6},
  xticklabels={ind-pos, ind-sgn, corr, anti, near, exact},
  xticklabel style={rotate=35, anchor=east, font=\tiny}, pcfplot}}
\pgfplotsset{rhoaxis/.style={xlabel={$\varrho$},
  xtick={0.3,0.6,0.9,1.0}, xmin=0.25, xmax=1.05, pcfplot}}
\pgfplotsset{sizeaxis/.style={xlabel={$n$}, /pgfplots/xmode=log, /pgfplots/ymode=log, pcfplot}}

\newcommand{\tab}[1]{\input{../results/tables/#1.tex}}
\newcommand{\dat}[1]{../results/tables/rep_plot_#1.dat}
\newcommand{\dfile}[1]{../results/tables/#1.dat}

% Every result is presented the same way: the numbers on the left, the same
% data plotted on the right, comment below.
\newcommand{\tabfig}[4]{%
  \begin{figure}[htbp]
  \centering
  \begin{minipage}[c]{0.46\textwidth}\centering\scriptsize
  \setlength{\tabcolsep}{3pt}#3\end{minipage}%
  \hfill
  \begin{minipage}[c]{0.50\textwidth}\centering#4\end{minipage}
  \caption{#2}\label{#1}
  \end{figure}}

\title{\vspace{-1.4cm}Parametric Maximum Closure on Directed Forests\\
\large Computational Report -- data release \texttt{v0.3.0}}
\author{Valerio Dose \and Fabio Furini \and Marco Locatelli}
\date{1 September 2026}

\begin{document}
\maketitle

\noindent
This report accompanies the manuscript \emph{``On parametric Maximum Closure
Problems over precedence forests''}. The manuscript states the algorithms,
their analysis, and the few experiments needed to support the theory. This
report is the complement: the full experimental record, and specifically the
material the manuscript does not carry --
\begin{itemize}
  \item absolute running times of \emph{every} algorithm at \emph{every} size
  of every campaign, not a selection;
  \item breakdowns by \emph{arc density} and by \emph{coefficient family},
  which is where the interesting structure turns out to be
  (\cref{sec:ccamp});
  \item what the instance generators actually produced -- arcs, components,
  number of closure layers -- so that a reader can tell what each parameter
  buys (\cref{sec:instances});
  \item the dispersion of the raw measurements, so that any reported ratio can
  be compared against the noise it rests on (\cref{sec:dispersion});
  \item the implementation analysis behind the $\mathcal O(n)$ space bound
  (\cref{sec:heap}), and the exact protocol, including what ``peak RSS'' means
  and why memory is only quoted from single-algorithm runs.
\end{itemize}
Every table and plot is generated from the raw benchmark data by
\texttt{tools/build\_report.sh}; no number is hand-typed. Each figure shows
the numbers and the same data plotted, with a comment on how to read them.

\tableofcontents
\vspace{4mm}

\section{Summary}

\begin{itemize}
  \item \textbf{Sweep.} One frozen commit, one machine, 2026-08-31/09-01:
  16\,200 instances, 292\,080 timed runs, seven algorithms plus the external
  \texttt{BPPF} baseline. No run hit the 300\,s timeout or the 8\,GiB memory
  ceiling, so nothing is censored.
  \item \textbf{Correctness.} Zero cross-algorithm disagreements
  (\cref{sec:validation}), on top of an exhaustive enumeration oracle on small
  instances and sanitizer builds.
  \item \textbf{Random forests, paths, binary trees.} \texttt{HPaC} is the
  fastest of our algorithms: $2$--$13\times$ \texttt{RaC} depending on size and
  density, and faster than the direct scan \texttt{PaC} from $n\approx400$ on.
  \item \textbf{Stars.} The ordering inverts: \texttt{RaC} is
  $\approx250\times$ \texttt{HPaC} at $n=20\,000$. Any heap-based schedule
  pays $\Theta(n)$ maintenance per event on a hub; \texttt{PaC}, which
  maintains no candidate structure, resists an order of magnitude better than
  \texttt{HPaC}.
  \item \textbf{Single orientations.} \texttt{HIPaC}/\texttt{HOPaC} take
  $0.52$--$0.78$ of \texttt{HPaC}'s time, stable up to $n=10^5$.
  \item \textbf{Against pseudoflow.} \texttt{HPaC} is faster than
  \texttt{BPPF} by a median factor $3.3$ on forests with $n\le1\,000$, even
  with the breakpoints handed to \texttt{BPPF} for free; \texttt{BPPF} misses
  the exact layer sequence on 16 of 2\,400 instances, for precision reasons
  (\cref{sec:bppf}).
  \item \textbf{Two facts that organize everything else.} The \emph{topology}
  decides which algorithm wins -- density moves the margin by an order of
  magnitude and the star class inverts it outright -- while the
  \emph{coefficient family} decides only how much parametric structure exists,
  moving absolute times but never the ranking. And the data structure inside
  one algorithm can matter as much as the choice of algorithm: the two heap
  policies of \cref{sec:heap} differ by $4\times$ in time and two orders of
  magnitude in memory, at identical output and identical asymptotic bounds.
\end{itemize}

\section{The algorithms, in one page}\label{sec:algos}

All of them solve the same problem and return the same object: given a
directed forest with integer profits $p_i$ and strictly positive integer
weights $w_i$, the ordered sequence of closure layers of
$u(\lambda)=\max\{P(S)-\lambda W(S): S \text{ closed}\}$, with the exact
rational threshold of each layer. All of ours use exact integer arithmetic:
64-bit coefficients, 128-bit cross-multiplication for every ratio comparison,
no floating point in any decision path.

\begin{table}[htbp]
\centering
\caption{The algorithms compared in this report. ``Time'' and ``space'' are
worst-case bounds from the manuscript; the last column is what the experiments
below add.}
\label{tab:algos}
\small
\begin{tabular}{@{}llllp{4.5cm}@{}}
\toprule
& Applies to & Time & Space & Idea, and what we observe \\
\midrule
\texttt{PaC} & any forest & $\mathcal O(n^2)$ & $\mathcal O(n)$ &
Peel the current best final vertices, or contract the best arc; the best
candidate is found by a linear scan at every iteration. Slowest on random
forests beyond $n\approx400$, but the most robust on stars. \\
\texttt{DPaC} & any forest & $\mathcal O(n^2)$ & $\mathcal O(n)$ &
Dual of \texttt{PaC}: builds the sequence from the lowest ratio upwards.
Tracks \texttt{PaC} everywhere. \\
\texttt{HPaC} & any forest & $\mathcal O(n^2\log n)$ & $\mathcal O(n)$ &
Same two moves, but the candidates live in two heaps, so only those touched by
the last move are refreshed. Fastest of ours on random forests, paths and
binary trees; penalized on a high-degree hub. \\
\texttt{DHPaC} & any forest & $\mathcal O(n^2\log n)$ & $\mathcal O(n)$ &
Dual of \texttt{HPaC}; within $25\%$ of it everywhere. \\
\texttt{HIPaC} & in-forests & $\mathcal O(n\log n)$ & $\mathcal O(n)$ &
When every vertex has out-degree $\le1$ the minimal preceding set of an arc is
a singleton, so one heap value per vertex suffices and no closure sums are
propagated. Takes $0.52$--$0.75$ of \texttt{HPaC}'s time. \\
\texttt{HOPaC} & out-forests & $\mathcal O(n\log n)$ & $\mathcal O(n)$ &
The symmetric case (in-degree $\le1$): $0.57$--$0.78$ of \texttt{HPaC}. \\
\texttt{RaC} & any tree & $\mathcal O(n\log n)$ & $\mathcal O(n\log n)$ &
Rake-and-compress on a top tree: contracts the tree in
$\mathcal O(\log n)$ rounds computing cluster functions, then recovers the
thresholds top-down. The only algorithm whose cost does not depend on how
often a single vertex is touched -- hence the winner on stars, by up to
$250\times$. \\
\midrule
\texttt{BPPF} & general graphs & $\mathcal O(mn\log n)$ & $\mathcal O(m)$ &
External baseline: bounded-precision parametric pseudoflow (unmodified
upstream), solves a strictly more general problem. The bound is for the whole
parametric sweep on a graph with $m$ arcs; on a forest $m=\mathcal O(n)$, so it
reads $\mathcal O(n^2\log n)$ -- worse than every algorithm above, which is the
theoretical reason a forest-specific method is worth having. Evaluates minimum cuts at parameter values supplied by
the caller and uses fixed-point arithmetic, hence the precision caveat of
\cref{sec:bppf}. \\
\bottomrule
\end{tabular}
\end{table}

\noindent
Two remarks that matter for reading the results. First, \texttt{PaC} and
\texttt{HPaC} are the \emph{same algorithm} under two schedules -- the
difference measured in \cref{sec:bcamp} is entirely the cost of finding the
maximum-ratio candidate. Second, \texttt{RaC} pays a structural overhead per
component (cluster bookkeeping) that the others do not, which is why density
matters so much in \cref{sec:ccamp}: a sparse forest is many small components.

\section{Definitions and conventions}\label{sec:defs}

Terms used throughout, fixed here once.

\begin{itemize}
  \item \textbf{Closure layer.} One block $\mathcal I_r$ of the canonical
  partition of the vertex set induced by the parametric solution: the vertices
  that enter the optimal closed set exactly when $\lambda$ crosses the
  breakpoint $\lambda_r$. ``Number of layers'' equals the number of
  breakpoints of $u(\lambda)$, so it measures how much parametric structure an
  instance has, and it bounds the number of iterations of every algorithm here.
  \item \textbf{Threshold} of a vertex: the breakpoint at which that vertex
  enters the optimal closed set; vertices with equal threshold form one layer.
  \item \textbf{Density $\varrho$.} The probability with which the generator
  adds each candidate arc. A forest with density $\varrho$ has $\varrho(n-1)$
  arcs and $n-\varrho(n-1)$ connected components in expectation, so
  $\varrho=1.0$ gives one spanning tree and $\varrho=0.3$ a forest of many
  small trees (\cref{fig:struct}).
  \item \textbf{Coefficient family.} The rule used to draw the profits and
  weights of an instance; six are used (\cref{sec:instances}). It controls the
  \emph{shape} of the ratios $p_i/w_i$ and hence how many layers appear.
  \item \textbf{Paired ratio.} The ratio of two algorithms' times
  \emph{on the same instance}. Reported as the median over instances, never as
  a ratio of two aggregates: pairing removes the instance-to-instance variation
  that would otherwise swamp the comparison.
  \item \textbf{IQR (interquartile range).} $Q_3-Q_1$: the width of the band
  containing the central half of the observations. It is to the median what the
  standard deviation is to the mean, but insensitive to the tails. Beside a
  median ratio, a large IQR signals that the population mixes regimes instead
  of scattering around one value -- exactly what happens across densities in
  \cref{sec:ccamp}.
  \item \textbf{Relative IQR} (\cref{sec:dispersion}): the IQR of the
  repetitions of one instance divided by that instance's median time, in
  percent. It quantifies measurement noise, not algorithmic variability.
  \item \textbf{Peak RSS} (resident set size): the maximum amount of physical
  RAM the process had mapped during the run, from
  \texttt{getrusage(RUSAGE\_SELF).ru\_maxrss}. See \cref{sec:setup} for the
  two caveats that make it usable only for single-algorithm runs.
  \item \textbf{Censored run.} A run killed by the time limit (300\,s) or the
  memory ceiling (8\,GiB), hence without a measured time. There are none in
  this study; the term appears only to state that.
  \item \textbf{Campaign.} One preregistered block of the sweep -- a set of
  instances plus the algorithms run on them (B, C, D, E, G).
\end{itemize}

\section{Setup}\label{sec:setup}

\paragraph{Machine and build.} Intel Core i7-12700, 32\,GB RAM, Ubuntu
22.04.5 (kernel 6.8), GCC 11.4.0 at \texttt{-O3}, single-threaded. Each
benchmark process pinned to one physical core (\texttt{taskset}); the six
campaign lanes ran on distinct cores, never two on the same core. CPU
governor \texttt{powersave} (no passwordless root on this machine), so
absolute times carry ordinary frequency-scaling noise -- which is one reason
every comparison is a paired same-instance ratio.

\paragraph{What is timed.} Only the algorithm call
(\texttt{std::chrono::steady\_clock}); parsing, result hashing and correctness
comparison sit outside the timed region. Repetitions: 11 for random forests
with $n\le1\,000$, 3 elsewhere. Algorithm order shuffled per repetition, so no
algorithm is systematically favoured by the CPU frequency state.

\paragraph{Memory, and what ``peak RSS'' means.} RSS is the \emph{resident set
size}: the amount of physical RAM the process has mapped at a given instant.
Peak RSS is its maximum over the run, read from
\texttt{getrusage(RUSAGE\_SELF).ru\_maxrss}. Two properties matter when
reading memory figures. It is a \emph{process-lifetime} peak and never
decreases, so in an invocation that benchmarks several algorithms the value
recorded for the second includes the peak of the first -- memory is therefore
quoted only from single-algorithm invocations (\cref{sec:heap}). And it counts
pages actually resident, so it includes allocator overhead and is precisely
the quantity that decides whether a run survives a memory ceiling.

\paragraph{Statistics.} Absolute times: median over repetitions, then median
over instances. Comparisons: \emph{paired} per instance, reported as the
median of the per-instance ratios together with their IQR
(\cref{sec:defs}), never a ratio of aggregate means.
The median is chosen because it is invariant under inversion of the comparison
(the arithmetic mean of ratios is not: it depends on which algorithm sits in
the denominator), because the timing noise has an asymmetric right tail by
construction (interference can only slow a run down, never speed it up), and
because it is the conservative choice on every comparison where we claim an
advantage -- on the \texttt{BPPF} comparison at $n=1\,000$ the three
aggregates are $4.5$ (median), $5.8$ (geometric mean) and $8.7$ (arithmetic
mean).

\section{Instances}\label{sec:instances}

An instance of this problem is two independent things: a \emph{precedence
structure} (which vertex forces which) and a set of \emph{affine
coefficients} (a profit and a weight per vertex). We generate the two
separately and cross them, so that any effect observed can be attributed to
one or the other. Concretely, one instance is fully determined by four
choices: a topology, a coefficient family, a size $n$, and a random seed.
Nothing else is tuned -- in particular there is no capacity, no time horizon
and no scaling of the coefficients.

Files use the \texttt{.pcf} format: the arc list, one integer profit and one
strictly positive integer weight per vertex, where arc $(u,v)$ encodes the
implication $x_u\le x_v$. The format has no capacity field and no reader
accepts one -- a deliberate choice, since the same algorithms exist in the
literature for capacitated variants and we did not want the two to be
confusable. Every instance is written by a seeded deterministic Python
generator, never by hand, and is recorded in a manifest with its SHA-256
checksum, its topology classification and its coefficient bounds; a
regenerated or downloaded archive is therefore verifiable byte-for-byte
against the one used here.

Below, each of the two components is described with the rule that generates
it. \Cref{sec:layersize} then measures what the combination actually produces
-- how many closure layers, and how big -- which is the quantity that governs
how much work every algorithm has to do.

\paragraph{Coefficient families (six).}
\begin{itemize}
  \item \texttt{independent-positive}: $w_i,p_i$ independent uniform on
  $\{1,\dots,1000\}$.
  \item \texttt{independent-signed}: $w_i$ as above, $p_i$ uniform on
  $\{-1000,\dots,1000\}$.
  \item \texttt{correlated}: $p_i=w_i+\delta_i$, $\delta_i$ uniform on
  $\{-50,\dots,50\}$.
  \item \texttt{anti-correlated}: $p_i=(1001-w_i)+\delta_i$.
  \item \texttt{near-ties}: one target ratio $\mathrm{num}/\mathrm{den}$ per
  instance ($\mathrm{num},\mathrm{den}$ uniform on $\{1,\dots,20\}$), then
  $t_i$ uniform on $\{1,\dots,50\}$, $w_i=\mathrm{den}\,t_i$,
  $p_i=\mathrm{num}\,t_i+\jmath_i$ with jitter $\jmath_i\in\{-2,-1,1,2\}$.
  \item \texttt{exact-ties}: $g=\max(1,\lfloor n/20\rfloor)$ groups each with
  its own exact ratio; vertex $i$ goes to group $i\bmod g$, so equal-threshold
  vertices are spread over the graph rather than adjacent.
\end{itemize}

\paragraph{Topologies (six).} \texttt{mixed-forest}: each $v\ge2$ linked to a
uniform predecessor $u<v$ with probability $\varrho$, each edge oriented
$(u,v)$ or $(v,u)$ with probability $1/2$. \texttt{in-forest}: one arc
$(u,j)$ with $j$ uniform above $u$, with probability $\varrho$ (out-degree
$\le1$); \texttt{out-forest} symmetric (in-degree $\le1$).
\texttt{path-mixed}, \texttt{binary-mixed}, \texttt{star-mixed}: fixed
underlying shape (path; balanced binary tree, parent
$\lfloor(v-1)/2\rfloor$; star, hub $0$), orientations randomized.

\paragraph{Sizes and counts.} 240 instances per topology and size (four
densities $\times$ six families $\times$ ten seeds), 60 for the structured
families, which have no density parameter. The random topologies use the ten
sizes $100,200,\dots,1\,000$ and the ten sizes
$10\,000,20\,000,\dots,100\,000$; the structured families use
$100$, $200$, $500$, $1\,000$, $2\,000$, $5\,000$, $10\,000$, $20\,000$,
$50\,000$ and $100\,000$.

\tabfig{fig:struct}{%
What the density parameter actually produces (campaign C, medians). Arcs grow
linearly in $\varrho$ and components fall accordingly, from a forest of
$\approx38\,500$ trees at $\varrho=0.3$ to a single spanning tree at
$\varrho=1.0$. The number of closure layers moves the other way and far less:
connecting the forest removes only about a third of them, because precedence
constraints merge layers that would otherwise be distinct. This is why density
is a genuine experimental factor and not a cosmetic one.}{%
\tab{rep_struct_by_rho}}{%
\begin{tikzpicture}
\begin{axis}[rhoaxis, ylabel={count}, /pgfplots/ymode=log]
\addplot[aux]  table[x=rho, y=arcs]   {\dfile{rep_plotstruct}}; \addlegendentry{arcs}
\addplot[aux2] table[x=rho, y=comp]   {\dfile{rep_plotstruct}}; \addlegendentry{components}
\addplot[hpac] table[x=rho, y=layers] {\dfile{rep_plotstruct}}; \addlegendentry{layers}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:layersfam}{%
How much parametric structure each coefficient family creates. This is the
single most useful fact for reading absolute times, since the work of every
algorithm scales with the number of breakpoints: at $n\ge10^4$ the median
layer count ranges from $1\,222$ (\texttt{near-ties}) to $33\,183$
(\texttt{independent-signed}), a factor of 27 at equal size. It also explains
why the tie-stressing families are the \emph{fastest} in every timing table
below, which would otherwise look counter-intuitive.}{%
\tab{rep_layers_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={\# closure layers}, /pgfplots/ymode=log]
\addplot[hpac, only marks] coordinates {(1,344) (2,346) (3,337) (4,342) (5,164) (6,105)};
\addlegendentry{$n\le10^3$}
\addplot[aux, only marks] coordinates {(1,32374) (2,33183) (3,24825) (4,30379) (5,1222) (6,7884)};
\addlegendentry{$n\ge10^4$}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:layerssize}{%
Closure layers by size over both random campaigns, with the observed spread.
The layer-to-vertex ratio settles around $0.15$ at $n=10^5$: a
$10^5$-vertex instance typically has of the order of $15\,000$ breakpoints to
enumerate, and the spread between the easiest and hardest instance of one size
is roughly a factor of three.}{%
\tab{rep_layers_by_size}}{%
\begin{tikzpicture}
\begin{axis}[sizeaxis, ylabel={\# closure layers}]
\addplot[hpac] table[x=n, y=median] {\dfile{rep_plotlayers}}; \addlegendentry{median}
\addplot[aux]  table[x=n, y=max]    {\dfile{rep_plotlayers}}; \addlegendentry{max}
\addplot[aux2] table[x=n, y=min]    {\dfile{rep_plotlayers}}; \addlegendentry{min}
\end{axis}
\end{tikzpicture}}

\subsection{How big are the closure layers?}\label{sec:layersize}

The campaign CSVs record how \emph{many} layers an instance has, not how big
they are: the algorithms are timed and their output is discarded. The size
distribution is worth one measurement of its own, because it is what a peel
step actually consumes. \Cref{fig:layersize} reports it on a sample of 60
instances at $n=10\,000$ (all six families, four densities, plus stars, paths
and in-forests), 261\,232 layers in total.

\tabfig{fig:layersize}{%
Size of the closure layers, measured on 60 instances at $n=10\,000$
(261\,232 layers). \emph{The distribution is extremely skewed}: overall
median 1, mean $2.3$, and two thirds of all layers are singletons -- yet the
largest single layer holds $5\,077$ vertices, half the graph. So a parametric
solution is typically a long chain of one-vertex layers plus a handful of very
large blocks, and an algorithm's cost per layer matters more than its cost per
vertex. Two group-level facts stand out. The tie-stressing families behave
completely differently from the others: \texttt{near-ties} has median layer
size 7 and \emph{no} singletons at all (by construction, vertices share
thresholds), against median 1 and $73$--$75\%$ singletons for the independent
families -- which is the mechanism behind their much smaller layer counts
(\cref{fig:layersfam}). And density compresses the largest layer by an order
of magnitude: on sparse random forests the biggest layer holds $563$ vertices,
against $\approx5\,000$ once the graph is a single tree, because a layer cannot
straddle components.}{%
\tiny\setlength{\tabcolsep}{2pt}\tab{rep_layer_sizes}}{%
\begin{tikzpicture}
\begin{axis}[sizeaxis, xlabel={layer size $s$},
  ylabel={\% of layers with size $\ge s$}]
\addplot[hpac, mark=none, very thick] table[x=size, y=frac_at_least]
  {\dfile{rep_plotlayersize}};
\addlegendentry{all 261\,232 layers}
\end{axis}
\end{tikzpicture}}

\section{Validation}\label{sec:validation}

\begin{itemize}
  \item \textbf{Exhaustive enumeration oracle} (CTest): every directed forest
  with $\le4$ vertices over a finite coefficient grid; 10\,000 random forests
  up to 11 vertices; mixed-orientation path, binary and star trees up to 257
  vertices; 6\,000 in-forest and 6\,000 out-forest instances plus 2\,000
  larger ones per orientation; plus the structural invariants (layers
  partition $V$, each is a closure increment, thresholds strictly decreasing).
  \item \textbf{Cross-algorithm differential agreement}: every campaign runs
  several algorithms on the same instance and compares partition, thresholds
  and canonical order through a 64-bit fingerprint. Over the sweep:
  \emph{zero} disagreements (\cref{tab:correctness}).
  \item \textbf{Sanitizers and CI}: GCC Release, GCC Debug with the address
  and undefined-behaviour sanitizers, and Clang Debug; all three pass at the
  frozen commit (\path{results/TEST_REPORT_2026-08-31.md}).
  \item \textbf{Overflow}: \texttt{validate\_instance} refuses any instance
  whose total absolute profit or weight would leave the exact-arithmetic
  margin ($\texttt{INT64\_MAX}/4$), before any algorithm runs.
\end{itemize}

\begin{table}[htbp]
\centering
\caption{Instances benchmarked and cross-algorithm disagreements per campaign.
Campaign G (\cref{sec:bppf}) is listed apart, \texttt{BPPF} being an external
baseline rather than one of our algorithms.}
\label{tab:correctness}
\small
\tab{correctness}
\end{table}

\section{The campaigns}\label{sec:campaigns}

A \emph{campaign} is one block of the sweep: a set of instances, the
algorithms run on them, and the number of repetitions. The split is not
cosmetic -- each campaign answers one question, and keeping them separate is
what allows a result to be attributed to a cause:

\begin{itemize}
  \item \textbf{A} is correctness only, on instances small enough to be
  solved by brute force, so that every algorithm can be checked against an
  independent oracle rather than against another algorithm.
  \item \textbf{B} and \textbf{C} are the same random topology at two scales.
  B is where all five algorithms still fit, so it is the one place where every
  pairwise comparison is available; C drops the two quadratic ones and asks how
  the rest scale.
  \item \textbf{D} fixes the shape (path, balanced binary tree, star) and
  varies nothing but orientations and coefficients, which isolates the effect
  of topology -- and produces the one case where the theoretical separation
  between our algorithms becomes visible.
  \item \textbf{E} restricts the orientation (in-forest, out-forest) to
  measure what a specialized algorithm gains from knowing it in advance.
  \item \textbf{G} is the external comparison against parametric pseudoflow.
  It sits on the same instances as B, so its numbers can be read against the
  rest of the study.
\end{itemize}

\noindent
All campaigns were frozen in \texttt{docs/EXPERIMENTAL\_PLAN\_V3.md} before any
run, together with the two size cutoffs, so that no scoping decision was taken
after seeing results. \Cref{tab:campaigns} gives the definitions,
\cref{tab:runs} how much work each algorithm actually did, and
\cref{tab:correctness} the correctness outcome per campaign.

\begin{table}[htbp]
\centering
\caption{Campaign definitions. Every campaign uses all six coefficient
families and ten seeds; the random topologies use all four densities. Two
preregistered size cutoffs apply, in both cases because the asymptotic trend
is already established below them and running further would consume hours
without adding information: \texttt{PaC}/\texttt{DPaC} stop at $n=20\,000$ on
random forests, and \texttt{HPaC}/\texttt{DHPaC} stop at $n=20\,000$ on stars
(where \texttt{PaC} is cheap and runs the full range instead).}
\label{tab:campaigns}
\begin{adjustbox}{max width=\textwidth}
\small
\begin{tabular}{@{}llllrl@{}}
\toprule
& Topology & Sizes $n$ & Inst./size & Reps & Algorithms \\
\midrule
A & all six & $\le11$ (exhaustive at 4) & -- & -- &
all, against the enumeration oracle \\
B & \texttt{mixed-forest} & $100,200,\dots,1\,000$ & 240 & 11 &
\texttt{PaC}, \texttt{DPaC}, \texttt{HPaC}, \texttt{DHPaC}, \texttt{RaC} \\
C & \texttt{mixed-forest} & $10\,000,\dots,100\,000$ & 240 & 3 &
\texttt{HPaC}, \texttt{DHPaC}, \texttt{RaC}; \texttt{PaC}, \texttt{DPaC} at
$10^4$, $2\cdot10^4$ \\
D & \texttt{path-}, \texttt{binary-}, \texttt{star-mixed} & 10 sizes,
$100\dots100\,000$ & 60 & 3 &
\texttt{HPaC}, \texttt{RaC}, \texttt{DHPaC}, \texttt{PaC}, \texttt{DPaC} \\
E & \texttt{in-}, \texttt{out-forest} & 20 sizes,
$100\dots100\,000$ & 240 & 3 &
\texttt{HPaC}, \texttt{DHPaC}, \texttt{HIPaC}/\texttt{HOPaC}, \texttt{RaC} \\
G & \texttt{mixed-forest} & $100,200,\dots,1\,000$ & 240 & 3 &
\texttt{HPaC} against \texttt{BPPF} \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

\begin{table}[htbp]
\centering
\caption{Work actually done per algorithm over the sweep. The
runs-per-instance column reflects the mix of repetition counts and cutoffs:
\texttt{PaC} and \texttt{DPaC} appear on fewer instances (size cutoffs) but
with more repetitions each, since they were mostly exercised on the medium
campaign where 11 repetitions were used. \texttt{RaC} appears on the most
instances, being the only algorithm run on every class at every size.}
\label{tab:runs}
\small
\tab{rep_runs_per_algorithm}
\end{table}

\section{Random forests}\label{sec:random}

The two random campaigns answer two different questions. At medium sizes the
question is whether the heap is worth its maintenance cost
(\cref{sec:bcamp}); at large sizes it is how the three surviving algorithms
scale, and how much the answer depends on the density of the forest
(\cref{sec:ccamp}).

\subsection{Medium sizes: direct scan versus heap}\label{sec:bcamp}

Campaign B runs all five of our algorithms on the same 2\,400 instances, which
makes it the one place where the primal/dual and scan/heap choices can be
compared without any confounding factor. It is also the size range where the
heap's overhead is still comparable to its benefit, so it is where the
crossover can be located.

\tabfig{fig:b}{%
All five algorithms on \texttt{mixed-forest} instances,
$n\in\{100,200,\dots,1\,000\}$ (240 instances per size). The two families are
indistinguishable up to $n\approx300$ and the heap pays off from $n\approx400$
on; each dual tracks its primal throughout, which is why the manuscript
reports only the primals. \texttt{PaC} re-scans every candidate ratio at every
iteration, while \texttt{HPaC} extracts the maximum in logarithmic time and
refreshes only the candidates touched by the last peel or contraction.}{%
\tab{rep_times_campaign_b}}{%
\begin{tikzpicture}
\begin{axis}[sizeaxis, ylabel={CPU time (ms)}]
\addplot[pac]   table[x=n, y=pac]   {\dat{campaign_b}}; \addlegendentry{\texttt{PaC}}
\addplot[dpac]  table[x=n, y=dpac]  {\dat{campaign_b}}; \addlegendentry{\texttt{DPaC}}
\addplot[hpac]  table[x=n, y=hpac]  {\dat{campaign_b}}; \addlegendentry{\texttt{HPaC}}
\addplot[dhpac] table[x=n, y=dhpac] {\dat{campaign_b}}; \addlegendentry{\texttt{DHPaC}}
\addplot[rac]   table[x=n, y=rac]   {\dat{campaign_b}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:b_ratio_pac}{%
Paired ratio \texttt{PaC}/\texttt{HPaC} by size, with its IQR: $0.91$ at
$n=100$ (the scan is marginally faster where heap maintenance does not yet pay
for itself), crossing 1 near $n=400$, reaching $2.3$ at $n=1\,000$. Beyond
this range it keeps growing -- $20.3$ at $n=10^4$, $42.9$ at $n=2\cdot10^4$ --
which is the practical meaning of the $\mathcal O(n^2)$ against
$\mathcal O(n\log n)$-in-practice difference. The dashed line marks parity.}{%
\tab{campaign_b_pac_over_hpac}}{%
\begin{tikzpicture}
\begin{axis}[pcfplot, xlabel={$n$}, ylabel={\texttt{PaC}/\texttt{HPaC}},
  /pgfplots/xmode=log, ymin=0,
  extra y ticks={1}, extra y tick style={grid=major, grid style={black!45, dashed}}]
\addplot[pac] table[x=n, y expr=\thisrow{pac}/\thisrow{hpac}] {\dat{campaign_b}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:b_rho}{%
Campaign B by density. Every algorithm slows down as the forest becomes more
connected, but not by the same factor: \texttt{HPaC} roughly doubles from
$\varrho=0.3$ to $\varrho=1.0$ while \texttt{RaC} grows less. This is the
medium-size beginning of the density effect that dominates
\cref{sec:ccamp}.}{%
\tab{rep_times_campaign_b_by_rho}}{%
\begin{tikzpicture}
\begin{axis}[rhoaxis, ylabel={CPU time (ms)}]
\addplot[pac]  table[x=rho, y=pac]  {\dfile{rep_plotrho_campaign_b}}; \addlegendentry{\texttt{PaC}}
\addplot[hpac] table[x=rho, y=hpac] {\dfile{rep_plotrho_campaign_b}}; \addlegendentry{\texttt{HPaC}}
\addplot[rac]  table[x=rho, y=rac]  {\dfile{rep_plotrho_campaign_b}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:b_fam}{%
Campaign B by coefficient family. The two tie-stressing families are the
fastest for every algorithm, consistently with their smaller layer counts
(\cref{fig:layersfam}); the ordering between algorithms is the same in all
six, so no family reverses a conclusion.}{%
\tab{rep_times_campaign_b_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={CPU time (ms)}]
\addplot[pac]  table[x=idx, y=pac]  {\dfile{rep_plotfam_campaign_b}}; \addlegendentry{\texttt{PaC}}
\addplot[hpac] table[x=idx, y=hpac] {\dfile{rep_plotfam_campaign_b}}; \addlegendentry{\texttt{HPaC}}
\addplot[rac]  table[x=idx, y=rac]  {\dfile{rep_plotfam_campaign_b}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:b_ratio_fam}{%
Campaign B, paired ratio \texttt{RaC}/\texttt{HPaC} by family: flat at
$3.1$--$3.3$ across all six. At these sizes the coefficient family does not
affect which algorithm wins.}{%
\tab{rep_ratio_campaign_b_rac_over_hpac_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={\texttt{RaC}/\texttt{HPaC}}, ymin=0]
\addplot[rac] table[x=idx, y=ratio] {\dfile{rep_plotratiofam_campaign_b_rac_hpac}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\subsection{Large sizes, and the effect of density}\label{sec:ccamp}

Campaign C drops the two direct-scan algorithms beyond $n=20\,000$ (their
quadratic growth is established by then) and asks the question that matters at
scale: how do \texttt{HPaC}, its dual and \texttt{RaC} compare on forests with
up to $10^5$ vertices, and how much of the answer depends on the density of
the forest. As it turns out, more than it depends on the size.

\tabfig{fig:c}{%
\texttt{mixed-forest} instances, $n\in\{10\,000,\dots,100\,000\}$ (240 per
size). \texttt{HPaC} computes the full parametric solution on $10^5$ vertices
in about $0.2$\,s; \texttt{DHPaC} stays within $25\%$ of it at every size, and
is marginally faster at $n=10^5$.}{%
\tab{rep_times_campaign_c}}{%
\begin{tikzpicture}
\begin{axis}[pcfplot, xlabel={$n$}, ylabel={CPU time (ms)},
  xtick={10000,40000,70000,100000}, scaled x ticks=false,
  /pgf/number format/.cd, fixed, 1000 sep={}, /tikz/.cd]
\addplot[rac]   table[x=n, y=rac]   {\dat{campaign_c}}; \addlegendentry{\texttt{RaC}}
\addplot[dhpac] table[x=n, y=dhpac] {\dat{campaign_c}}; \addlegendentry{\texttt{DHPaC}}
\addplot[hpac]  table[x=n, y=hpac]  {\dat{campaign_c}}; \addlegendentry{\texttt{HPaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:c_ratio}{%
Paired ratio \texttt{RaC}/\texttt{HPaC} by size, with IQR: from $4.2$ at
$n=10^4$ to $13.3$ at $n=10^5$. The IQR widens with $n$, so the disadvantage
grows \emph{and} becomes more variable -- the next figure explains why.}{%
\tab{campaign_c_rac_over_hpac}}{%
\begin{tikzpicture}
\begin{axis}[pcfplot, xlabel={$n$}, ylabel={\texttt{RaC}/\texttt{HPaC}},
  xtick={10000,40000,70000,100000}, scaled x ticks=false, ymin=0,
  /pgf/number format/.cd, fixed, 1000 sep={}, /tikz/.cd]
\addplot[rac] table[x=n, y expr=\thisrow{rac}/\thisrow{hpac}] {\dat{campaign_c}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:c_ratio_rho}{%
The same pairing aggregated by density, and the reason a single aggregate
would mislead: the median ratio is $12.9$ at $\varrho=0.3$, $17.0$ at
$\varrho=0.6$, $5.0$ at $\varrho=0.9$ and only $2.0$ on single spanning trees,
with the IQR shrinking from $10.2$ to $0.4$ in the same direction. A sparse
forest splits into many small components, on which \texttt{HPaC}'s
contractions are almost free while \texttt{RaC} still pays cluster bookkeeping
per component; on one spanning tree the two are closest. The ordering never
reverses, but the margin varies by almost an order of magnitude -- the
population is a mixture of four regimes, which is exactly what the widening
IQR of \cref{fig:c_ratio} is made of.}{%
\tab{campaign_c_rac_over_hpac_by_rho}}{%
\begin{tikzpicture}
\begin{axis}[rhoaxis, ylabel={\texttt{RaC}/\texttt{HPaC}}, ymin=0]
\addplot[rac] table[x=rho, y=ratio] {\dfile{rep_plotratiorho_campaign_c_rac_hpac}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:c_rho}{%
Campaign C by density, absolute times: the gap between \texttt{RaC} and the
heap-based algorithms closes as the forest becomes connected, and all three
get faster -- fewer components means fewer, larger closure layers
(\cref{fig:struct}).}{%
\tab{rep_times_campaign_c_by_rho}}{%
\begin{tikzpicture}
\begin{axis}[rhoaxis, ylabel={CPU time (ms)}]
\addplot[rac]   table[x=rho, y=rac]   {\dfile{rep_plotrho_campaign_c}}; \addlegendentry{\texttt{RaC}}
\addplot[dhpac] table[x=rho, y=dhpac] {\dfile{rep_plotrho_campaign_c}}; \addlegendentry{\texttt{DHPaC}}
\addplot[hpac]  table[x=rho, y=hpac]  {\dfile{rep_plotrho_campaign_c}}; \addlegendentry{\texttt{HPaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:c_fam}{%
Campaign C by family, absolute times: at most $14\%$ spread per algorithm --
an order of magnitude less than the spread across densities.}{%
\tab{rep_times_campaign_c_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={CPU time (ms)}]
\addplot[rac]   table[x=idx, y=rac]   {\dfile{rep_plotfam_campaign_c}}; \addlegendentry{\texttt{RaC}}
\addplot[dhpac] table[x=idx, y=dhpac] {\dfile{rep_plotfam_campaign_c}}; \addlegendentry{\texttt{DHPaC}}
\addplot[hpac]  table[x=idx, y=hpac]  {\dfile{rep_plotfam_campaign_c}}; \addlegendentry{\texttt{HPaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:c_ratio_fam}{%
Campaign C, paired ratio by family: between $5.80$ and $6.11$ in all six. Read
against \cref{fig:c_ratio_rho}, this is the clearest single statement of the
study -- the topology decides which algorithm wins, the coefficient family
only how much work there is to do.}{%
\tab{rep_ratio_campaign_c_rac_over_hpac_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={\texttt{RaC}/\texttt{HPaC}}, ymin=0]
\addplot[rac] table[x=idx, y=ratio] {\dfile{rep_plotratiofam_campaign_c_rac_hpac}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\subsection{What the random campaigns say}

Three things follow from \cref{fig:b,fig:b_ratio_pac,fig:c,fig:c_ratio,fig:c_ratio_rho,fig:c_ratio_fam}
and are worth stating explicitly, because none of them is visible from the
complexity bounds alone.

\begin{itemize}
  \item \textbf{The heap is worth it, but only past a size threshold.} Below
  $n\approx300$ the direct scan is as good and at $n=100$ marginally better:
  the heap's maintenance is not amortized yet. The threshold is small enough
  that it does not matter in practice, but it does mean that for tiny
  instances the simpler algorithm is the right choice.
  \item \textbf{Density is a first-order factor, and it acts against
  \texttt{RaC}.} The \texttt{RaC}/\texttt{HPaC} ratio moves from $2.0$ to
  $17.0$ as $\varrho$ goes from $1.0$ to $0.6$ -- a bigger swing than going
  from $n=10^4$ to $n=10^5$ at fixed density. Anyone benchmarking these
  algorithms on a single density would draw a substantially wrong conclusion
  about the margin. The reason is structural: \texttt{RaC} pays cluster
  bookkeeping per component, and a sparse forest is thousands of tiny
  components.
  \item \textbf{The coefficient family is a second-order factor.} It changes
  absolute times by at most $14\%$ and the paired ratio by less than $6\%$,
  even though it changes the number of closure layers by a factor of 27
  (\cref{fig:layersfam}). Time per layer, not time per vertex, is what the
  family moves -- and it moves it for all algorithms together.
\end{itemize}

\section{Structured classes}\label{sec:structured}

Paths, balanced binary trees and stars share one property that the random
forests do not: the topology is fixed, so only the orientations and the
coefficients vary. They therefore isolate the effect of the shape, and they
are the classes where the theoretical complexity separation becomes visible.
Paths and binary trees confirm the random-forest picture; stars invert it.

\tabfig{fig:path}{%
\texttt{path-mixed} instances (60 per size). These behave like random forests:
median \texttt{RaC}/\texttt{HPaC} between $5.8$ and $9.0$ over the whole
range, and \texttt{PaC} left far behind by $n=10^4$, as its
$\mathcal O(n^2)$ bound predicts.}{%
\tab{rep_times_campaign_d_path}}{%
\begin{tikzpicture}
\begin{axis}[sizeaxis, ylabel={CPU time (ms)}]
\addplot[pac]  table[x=n, y=pac]  {\dat{campaign_d_path}}; \addlegendentry{\texttt{PaC}}
\addplot[hpac] table[x=n, y=hpac] {\dat{campaign_d_path}}; \addlegendentry{\texttt{HPaC}}
\addplot[rac]  table[x=n, y=rac]  {\dat{campaign_d_path}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:path_fam}{%
Paths by family: absolute times (left) and the paired ratio
\texttt{RaC}/\texttt{HPaC} (right). Flat across families, like the random
campaigns.}{%
\tab{rep_times_campaign_d_path_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={\texttt{RaC}/\texttt{HPaC}}, ymin=0]
\addplot[rac] table[x=idx, y=ratio] {\dfile{rep_plotratiofam_campaign_d_path_rac_hpac}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:binary}{%
\texttt{binary-mixed} instances (60 per size): median
\texttt{RaC}/\texttt{HPaC} between $3.2$ and $3.9$ over the whole range --
lower than on paths, since a balanced tree gives \texttt{RaC}'s contraction
rounds more to work with.}{%
\tab{rep_times_campaign_d_binary}}{%
\begin{tikzpicture}
\begin{axis}[sizeaxis, ylabel={CPU time (ms)}]
\addplot[pac]  table[x=n, y=pac]  {\dat{campaign_d_binary}}; \addlegendentry{\texttt{PaC}}
\addplot[hpac] table[x=n, y=hpac] {\dat{campaign_d_binary}}; \addlegendentry{\texttt{HPaC}}
\addplot[rac]  table[x=n, y=rac]  {\dat{campaign_d_binary}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:binary_fam}{%
Binary trees by family: absolute times (left), paired ratio (right).}{%
\tab{rep_times_campaign_d_binary_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={\texttt{RaC}/\texttt{HPaC}}, ymin=0]
\addplot[rac] table[x=idx, y=ratio] {\dfile{rep_plotratiofam_campaign_d_binary_rac_hpac}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:star}{%
\texttt{star-mixed} instances (60 per size) -- the class that inverts the
ordering. \texttt{RaC} is already faster at $n=100$ and $\approx250\times$
faster at $n=20\,000$. Every peel or contraction touches the hub and
invalidates the candidate ratios of a constant fraction of the remaining
leaves, so \emph{any} heap-based schedule pays $\Theta(n)$ maintenance per
event, while \texttt{RaC}'s rake operations absorb all leaves in
$\mathcal O(\log n)$ rounds regardless of the hub's update history.
\texttt{PaC}, which maintains no candidate structure at all, stays an order of
magnitude below \texttt{HPaC}. The heap-based algorithms stop at the
preregistered cutoff $n=20\,000$, where their quadratic trend is
established.}{%
\tab{rep_times_campaign_d_star}}{%
\begin{tikzpicture}
\begin{axis}[sizeaxis, ylabel={CPU time (ms)}]
\addplot[hpac] table[x=n, y=hpac] {\dat{campaign_d_star}}; \addlegendentry{\texttt{HPaC}}
\addplot[pac]  table[x=n, y=pac]  {\dat{campaign_d_star}}; \addlegendentry{\texttt{PaC}}
\addplot[rac]  table[x=n, y=rac]  {\dat{campaign_d_star}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:star_fam}{%
Stars by family: absolute times (left) and the paired ratio
\texttt{RaC}/\texttt{HPaC} (right), between $0.060$ and $0.064$ in all six.
The inversion is purely topological: no coefficient family produces it and
none removes it.}{%
\tab{rep_times_campaign_d_star_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={\texttt{RaC}/\texttt{HPaC}}, ymin=0]
\addplot[rac] table[x=idx, y=ratio] {\dfile{rep_plotratiofam_campaign_d_star_rac_hpac}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:star_racpac}{%
Stars, \texttt{RaC} against \texttt{PaC}: the comparison between the two
algorithms that are \emph{not} penalized by a candidate structure on a hub,
and therefore the one that isolates the asymptotic difference. The crossover
sits between $n=1\,000$ and $n=2\,000$; by $n=10^5$ \texttt{RaC} is
$130\times$ faster. Dashed line marks parity.}{%
\tab{campaign_d_star_rac_over_pac}}{%
\begin{tikzpicture}
\begin{axis}[sizeaxis, ylabel={\texttt{RaC}/\texttt{PaC}},
  extra y ticks={1}, extra y tick style={grid=major, grid style={black!45, dashed}}]
\addplot[rac] table[x=n, y expr=\thisrow{rac}/\thisrow{pac}] {\dat{campaign_d_star}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\subsection{What the structured classes say}

\begin{itemize}
  \item \textbf{Paths and binary trees add no new regime.} The ratios sit
  where the random forests put them ($5.8$--$9.0$ and $3.2$--$3.9$) and are
  flat in $n$ over three orders of magnitude: on these shapes the asymptotic
  advantage of \texttt{RaC} never materializes within the sizes we can run.
  \item \textbf{Stars are the counterexample the theory predicts, and they
  are extreme.} The ratio moves by four orders of magnitude across the size
  range, from $0.75$ to $0.004$. This is the one place in the study where the
  complexity separation is not academic.
  \item \textbf{The star penalty is a property of the \emph{schedule}, not of
  the algorithm.} \texttt{PaC} and \texttt{HPaC} implement the same
  algorithm; on stars the heap-based schedule is an order of magnitude slower
  than the scan-based one (\cref{fig:star}), because maintaining candidate
  ratios is exactly what a hub makes expensive. A practitioner facing
  hub-shaped precedence graphs should reach for \texttt{RaC}, and failing that
  for the direct scan -- not for the heap.
\end{itemize}

\section{Single orientations}\label{sec:oriented}

When the forest is known in advance to be a forest of in-trees or of
out-trees, a specialized algorithm can exploit it. This section measures how
much that knowledge is worth: it is a constant factor, not a change of regime,
and the constant is remarkably stable across sizes, densities and coefficient
families.

\tabfig{fig:ein}{%
\texttt{in-forest} instances (240 per size): the specialized \texttt{HIPaC}
against the general \texttt{HPaC} and against \texttt{RaC}. On a single
orientation the minimal preceding set of every arc collapses to a singleton,
so one heap value per vertex suffices and no closure-sum propagation is ever
needed -- that is where the constant factor comes from.}{%
\tab{rep_times_campaign_e_in}}{%
\begin{tikzpicture}
\begin{axis}[sizeaxis, ylabel={CPU time (ms)}]
\addplot[hpac]  table[x=n, y=hpac]  {\dat{campaign_e_in}}; \addlegendentry{\texttt{HPaC}}
\addplot[hipac] table[x=n, y=hipac] {\dat{campaign_e_in}}; \addlegendentry{\texttt{HIPaC}}
\addplot[rac]   table[x=n, y=rac]   {\dat{campaign_e_in}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:ein_ratio}{%
Paired ratio \texttt{HIPaC}/\texttt{HPaC} by size: between $0.52$ and $0.75$,
with no erosion up to $n=10^5$ -- if anything the advantage grows mildly with
$n$. Dashed line marks parity.}{%
\tab{campaign_e_in_hipac_over_hpac}}{%
\begin{tikzpicture}
\begin{axis}[pcfplot, xlabel={$n$}, /pgfplots/xmode=log, ylabel={\texttt{HIPaC}/\texttt{HPaC}},
  ymin=0, ymax=1.1,
  extra y ticks={1}, extra y tick style={grid=major, grid style={black!45, dashed}}]
\addplot[hipac] table[x=n, y expr=\thisrow{hipac}/\thisrow{hpac}] {\dat{campaign_e_in}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:eout}{%
\texttt{out-forest} instances (240 per size): \texttt{HOPaC} against
\texttt{HPaC} and \texttt{RaC}. The picture mirrors the in-forest one, as the
symmetry of the two constructions predicts.}{%
\tab{rep_times_campaign_e_out}}{%
\begin{tikzpicture}
\begin{axis}[sizeaxis, ylabel={CPU time (ms)}]
\addplot[hpac]  table[x=n, y=hpac]  {\dat{campaign_e_out}}; \addlegendentry{\texttt{HPaC}}
\addplot[hopac] table[x=n, y=hopac] {\dat{campaign_e_out}}; \addlegendentry{\texttt{HOPaC}}
\addplot[rac]   table[x=n, y=rac]   {\dat{campaign_e_out}}; \addlegendentry{\texttt{RaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:eout_ratio}{%
Paired ratio \texttt{HOPaC}/\texttt{HPaC} by size: between $0.57$ and
$0.78$.}{%
\tab{campaign_e_out_hopac_over_hpac}}{%
\begin{tikzpicture}
\begin{axis}[pcfplot, xlabel={$n$}, /pgfplots/xmode=log, ylabel={\texttt{HOPaC}/\texttt{HPaC}},
  ymin=0, ymax=1.1,
  extra y ticks={1}, extra y tick style={grid=major, grid style={black!45, dashed}}]
\addplot[hopac] table[x=n, y expr=\thisrow{hopac}/\thisrow{hpac}] {\dat{campaign_e_out}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:e_rho}{%
In-forests by density: absolute times (left) and the specialized-variant ratio
(right). The gain of the specialization is stable in $\varrho$ too, so it does
not depend on how fragmented the forest is.}{%
\tab{rep_times_campaign_e_in_by_rho}}{%
\begin{tikzpicture}
\begin{axis}[rhoaxis, ylabel={\texttt{HIPaC}/\texttt{HPaC}}, ymin=0, ymax=1]
\addplot[hipac] table[x=rho, y=ratio] {\dfile{rep_plotratiorho_campaign_e_in_hipac_hpac}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:e_fam}{%
In-forests by family: the ratio table (left) and its plot (right). There is no
family in which the advantage of the specialization disappears.}{%
\tab{rep_ratio_campaign_e_in_hipac_over_hpac_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={\texttt{HIPaC}/\texttt{HPaC}}, ymin=0, ymax=1]
\addplot[hipac] table[x=idx, y=ratio] {\dfile{rep_plotratiofam_campaign_e_in_hipac_hpac}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:e_rac}{%
For completeness, \texttt{RaC} against the general \texttt{HPaC} on the same
oriented instances, by family (out-forests behave the same). \texttt{RaC}
loses ground as $n$ grows here too, from about $2.8\times$ at $n\le1\,000$ to
$12.8\times$ at $n=10^5$.}{%
\tab{rep_ratio_campaign_e_in_rac_over_hpac_by_family}}{%
\begin{tikzpicture}
\begin{axis}[famaxis, ylabel={\texttt{RaC}/\texttt{HPaC}}, ymin=0]
\addplot[rac] table[x=idx, y=ratio] {\dfile{rep_plotratiofam_campaign_e_in_rac_hpac}};
\addlegendentry{median ratio}
\end{axis}
\end{tikzpicture}}

\paragraph{Note on the comparison with the first version of the manuscript.}
The specialized variants were reported there as $4$--$10\times$ faster than
the general algorithm, against $1.3$--$1.9\times$ here. The direction is
expected -- the general \texttt{HPaC} is substantially faster than it was, see
\cref{sec:heap} -- but the magnitude is only partly accounted for by the heap
policy: comparing the two heap implementations directly on these instances
gives $1.3$--$1.4\times$, not $3$--$4\times$. The remainder is attributable to
the code being an independent rewrite rather than a refactoring, and to
instances regenerated under different coefficient families. Absolute times
across the two versions are therefore not comparable; only trends are.

\subsection{What the oriented classes say}

\begin{itemize}
  \item \textbf{The specialization buys a stable constant, around $1.5\times$
  to $2\times$.} It does not change the growth rate, and it does not erode:
  the ratio at $n=10^5$ is as good as at $n=10^3$, and it is the same across
  densities and families. This is the cleanest, most predictable effect in the
  whole study.
  \item \textbf{It is smaller than the theory might suggest.} Both
  \texttt{HIPaC} and the general \texttt{HPaC} behave like
  $\mathcal O(n\log n)$ in practice on these instances, so what separates them
  is bookkeeping, not asymptotics: one heap value per vertex instead of two
  heaps plus closure-sum propagation.
  \item \textbf{Knowing the orientation is worth less than knowing the
  shape.} A factor $1.5$--$2$ for a single orientation, against a factor $250$
  for a star. If one piece of structural information about the input can be
  exploited, the shape is the one that pays.
\end{itemize}

\section{Implementation note: heap policy}\label{sec:heap}

The candidate heaps admit two implementations, indistinguishable in the
asymptotic time bound and very different in practice.

\begin{itemize}
  \item \textbf{Lazy deletion (push-only).} Every closure-sum change pushes a
  fresh entry; stale ones are dropped only when they surface at the top. Heap
  size is bounded by the number of touch operations, not by $n$. On a star this
  is pathological: every event touches the hub, each touch re-pushes one entry
  per incident arc, so the heap accumulates $\Theta(n^2)$ entries.
  \item \textbf{Periodic rebuild (used throughout this study).} Each heap is
  rebuilt from the live candidates whenever its size exceeds a constant
  multiple of the live candidate count: amortized $\mathcal O(\log n)$ per
  operation, worst-case time bound unchanged, memory $\mathcal O(n)$ on
  \emph{every} input. This is what makes the implementation attain the space
  bound proved in the manuscript.
\end{itemize}

\tabfig{fig:heap}{%
The two policies on \texttt{star-mixed} instances
(\texttt{independent-positive}, seed 0, one repetition; single-algorithm
invocations, the only regime in which peak RSS is attributable to one
algorithm). The lazy heap's memory grows by a factor of 100 when $n$ grows by
10 -- quadratic, as predicted -- reaching $1.5$\,GiB at $n=10^4$ where the
rebuild policy uses $8$\,MiB; extrapolating, the lazy policy exhausts an
8\,GiB ceiling by $n\approx20\,000$. The rebuild policy is also
$\approx4\times$ faster here (and $\approx2\times$ on large random forests),
with bit-identical output on every instance tested. For reference,
\texttt{PaC}, which maintains no candidate structure, peaks at $5.6$\,MiB at
$n=10^4$. The practical lesson: on this problem the choice of data structure
inside one algorithm is worth as much as the choice between algorithms.}{%
\begin{tabular}{@{}rrrrr@{}}
\toprule
& \multicolumn{2}{c}{lazy deletion} & \multicolumn{2}{c}{periodic rebuild} \\
\cmidrule(lr){2-3}\cmidrule(l){4-5}
$n$ & ms & MiB & ms & MiB \\
\midrule
1\,000 & 139.0 & 15.6 & 64.1 & 3.9 \\
2\,000 & 628.7 & 51.5 & 233.1 & 4.0 \\
5\,000 & 5\,440.9 & 388.0 & 1\,422.1 & 6.4 \\
10\,000 & 27\,020.7 & 1\,541.2 & 6\,396.5 & 8.2 \\
\bottomrule
\end{tabular}}{%
\begin{tikzpicture}
\begin{axis}[sizeaxis, ylabel={peak RSS (MiB)}]
\addplot[aux2] table[x=n, y=rss_lazy]    {\dfile{rep_plotheap}}; \addlegendentry{lazy}
\addplot[hpac] table[x=n, y=rss_bounded] {\dfile{rep_plotheap}}; \addlegendentry{rebuild}
\end{axis}
\end{tikzpicture}}

\section{Comparison with parametric pseudoflow}\label{sec:bppf}

\texttt{BPPF} evaluates minimum cuts at user-supplied parameter values, so the
comparison must decide what to supply. We drive it in the setting most
favourable to it, identical to the methodology of the manuscript's first
version: one \texttt{BPPF} process per instance receives, in \texttt{BPPF}'s
native affine capacity format, the $k+1$ values that bracket all $k$
breakpoints -- so it is spared the search for the breakpoints, and one call
certifies the whole parametric solution. Timing is \texttt{BPPF}'s own
internal solve timer (input parsing excluded) against \texttt{HPaC}'s
in-process time on the same instance; fixed-point precision $10^{-6}$; all
2\,400 instances with $n\le1\,000$.

\tabfig{fig:bppf}{%
\texttt{HPaC} against \texttt{BPPF}. The median paired ratio grows from $1.5$
at $n=100$ to $4.5$ at $n=1\,000$ (overall median $3.3$). Scope: forests with
$n\le1\,000$, while \texttt{BPPF} solves a strictly more general problem, so
this says nothing about \texttt{BPPF} outside this class. Note the times are
sub-millisecond at the small end, which is why they are quoted here to one
decimal in milliseconds and the ratio is computed per instance rather than
from these columns.}{%
\begin{tabular}{@{}rrrr@{}}
\toprule
& & \multicolumn{2}{c}{CPU time (ms)} \\
\cmidrule(l){3-4}
$n$ & \#inst & \texttt{HPaC} & \texttt{BPPF} \\
\midrule
100 & 240 & 0.1 & 0.1 \\
200 & 240 & 0.1 & 0.3 \\
300 & 240 & 0.2 & 0.6 \\
400 & 240 & 0.3 & 1.0 \\
500 & 240 & 0.4 & 1.5 \\
600 & 240 & 0.5 & 2.1 \\
700 & 240 & 0.5 & 2.8 \\
800 & 240 & 0.7 & 3.6 \\
900 & 240 & 0.8 & 4.5 \\
1\,000 & 240 & 0.9 & 5.5 \\
\bottomrule
\end{tabular}}{%
\begin{tikzpicture}
\begin{axis}[pcfplot, xlabel={$n$}, ylabel={CPU time (ms)}, /pgfplots/ymode=log,
  xtick={100,400,700,1000}, scaled x ticks=false]
\addplot[bppf] coordinates {(100,0.105) (200,0.305) (300,0.604) (400,0.991)
  (500,1.509) (600,2.086) (700,2.777) (800,3.581) (900,4.507) (1000,5.526)};
\addlegendentry{\texttt{BPPF}}
\addplot[hpac] coordinates {(100,0.061) (200,0.137) (300,0.221) (400,0.294)
  (500,0.382) (600,0.460) (700,0.530) (800,0.658) (900,0.752) (1000,0.871)};
\addlegendentry{\texttt{HPaC}}
\end{axis}
\end{tikzpicture}}

\paragraph{Agreement and precision.} The closures returned by the two methods
agree at every probe on every instance, with one exception class: on
\textbf{16 of the 2\,400 instances} \texttt{BPPF} merges two consecutive
closure layers whose thresholds differ by less than its fixed-point precision,
and therefore does not recover the exact optimal layer sequence. All 16 belong
to \texttt{near-ties} and \texttt{exact-ties}, the two families designed to
place thresholds at exactly that distance -- which is also why those families
exist. Our algorithms are unaffected, comparing integers exactly.

\paragraph{Discarded variant.} An earlier version of this comparison drove
\texttt{BPPF} once per breakpoint, one process spawn per call. It is reported
nowhere: the resulting ratio is dominated by process-creation overhead and
says nothing about either algorithm.

\section{Dispersion of the measurements}\label{sec:dispersion}

Every ratio reported above is a median over instances of a median over
repetitions. This section reports the second-level dispersion -- how much the
repetitions of a single instance differ from each other -- because that is the
noise floor against which every claimed difference has to be read. It is
expressed as the relative IQR (\cref{sec:defs}): the IQR over the repetitions
of one instance, divided by that instance's median time.

\tabfig{fig:iqr_c}{%
Median \emph{relative} IQR of the timing (IQR over the repetitions of one
instance, divided by that instance's median), campaign C. Below $1\%$ for
\texttt{HPaC} throughout, a few percent for \texttt{DHPaC} and \texttt{RaC}.
This is the number that says how much of a reported difference is signal:
every ratio in this report is far larger than the noise of the measurements it
rests on. The single exception is the near-tie $0.91$ against $1$ at $n=100$
in \cref{sec:bcamp}, which is reported as a near-tie and not as an
advantage.}{%
\tab{rep_iqr_campaign_c}}{%
\begin{tikzpicture}
\begin{axis}[pcfplot, xlabel={$n$}, ylabel={relative IQR (\%)},
  xtick={10000,40000,70000,100000}, scaled x ticks=false, ymin=0,
  /pgf/number format/.cd, fixed, 1000 sep={}, /tikz/.cd]
\addplot[rac]   table[x=n, y=rac]   {\dfile{rep_plotiqr_campaign_c}}; \addlegendentry{\texttt{RaC}}
\addplot[dhpac] table[x=n, y=dhpac] {\dfile{rep_plotiqr_campaign_c}}; \addlegendentry{\texttt{DHPaC}}
\addplot[hpac]  table[x=n, y=hpac]  {\dfile{rep_plotiqr_campaign_c}}; \addlegendentry{\texttt{HPaC}}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:iqr_e}{%
The same for in-forests: the specialized variant is as stable as the general
algorithm, so its advantage is not an artefact of variance.}{%
\tab{rep_iqr_campaign_e_in}}{%
\begin{tikzpicture}
\begin{axis}[pcfplot, xlabel={$n$}, ylabel={relative IQR (\%)}, /pgfplots/xmode=log, ymin=0]
\addplot[rac]   table[x=n, y=rac]   {\dfile{rep_plotiqr_campaign_e_in}}; \addlegendentry{\texttt{RaC}}
\addplot[hipac] table[x=n, y=hipac] {\dfile{rep_plotiqr_campaign_e_in}}; \addlegendentry{\texttt{HIPaC}}
\addplot[hpac]  table[x=n, y=hpac]  {\dfile{rep_plotiqr_campaign_e_in}}; \addlegendentry{\texttt{HPaC}}
\end{axis}
\end{tikzpicture}}

\appendix

\section{Per-family detail at every size}\label{app:detail}

The full cross-tabulation behind every by-family statement above: median CPU
time per coefficient family and per size, for the two headline algorithms, on
both random campaigns. The family ordering is stable across sizes -- the two
tie-stressing families are consistently the fastest for both algorithms, by a
margin that widens with $n$ (at $n=10^5$, \texttt{near-ties} costs
\texttt{HPaC} about $12\%$ less than \texttt{independent-positive}) -- and no
family ever reverses the relative standing of the two algorithms. The plots
show four representative families to stay readable; the tables give all six.

\tabfig{fig:detail_b_hpac}{%
\texttt{HPaC} on \texttt{mixed-forest} instances, $n\le1\,000$.}{%
\tab{rep_detail_campaign_b_hpac}}{%
\begin{tikzpicture}
\begin{axis}[sizeaxis, ylabel={\texttt{HPaC} time (ms)}, legend columns=2]
\addplot[hpac] table[x=n, y=independentpositive] {\dfile{rep_plotdetail_campaign_b_hpac}};
\addlegendentry{ind-pos}
\addplot[hpac, dashed, mark=o] table[x=n, y=correlated] {\dfile{rep_plotdetail_campaign_b_hpac}};
\addlegendentry{corr}
\addplot[aux] table[x=n, y=nearties] {\dfile{rep_plotdetail_campaign_b_hpac}};
\addlegendentry{near-ties}
\addplot[aux2] table[x=n, y=exactties] {\dfile{rep_plotdetail_campaign_b_hpac}};
\addlegendentry{exact-ties}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:detail_b_rac}{%
\texttt{RaC} on \texttt{mixed-forest} instances, $n\le1\,000$.}{%
\tab{rep_detail_campaign_b_rac}}{%
\begin{tikzpicture}
\begin{axis}[sizeaxis, ylabel={\texttt{RaC} time (ms)}, legend columns=2]
\addplot[rac] table[x=n, y=independentpositive] {\dfile{rep_plotdetail_campaign_b_rac}};
\addlegendentry{ind-pos}
\addplot[rac, dashed, mark=o] table[x=n, y=correlated] {\dfile{rep_plotdetail_campaign_b_rac}};
\addlegendentry{corr}
\addplot[aux] table[x=n, y=nearties] {\dfile{rep_plotdetail_campaign_b_rac}};
\addlegendentry{near-ties}
\addplot[aux2] table[x=n, y=exactties] {\dfile{rep_plotdetail_campaign_b_rac}};
\addlegendentry{exact-ties}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:detail_c_hpac}{%
\texttt{HPaC} on \texttt{mixed-forest} instances, $n\ge10\,000$.}{%
\tab{rep_detail_campaign_c_hpac}}{%
\begin{tikzpicture}
\begin{axis}[sizeaxis, ylabel={\texttt{HPaC} time (ms)}, legend columns=2]
\addplot[hpac] table[x=n, y=independentpositive] {\dfile{rep_plotdetail_campaign_c_hpac}};
\addlegendentry{ind-pos}
\addplot[hpac, dashed, mark=o] table[x=n, y=correlated] {\dfile{rep_plotdetail_campaign_c_hpac}};
\addlegendentry{corr}
\addplot[aux] table[x=n, y=nearties] {\dfile{rep_plotdetail_campaign_c_hpac}};
\addlegendentry{near-ties}
\addplot[aux2] table[x=n, y=exactties] {\dfile{rep_plotdetail_campaign_c_hpac}};
\addlegendentry{exact-ties}
\end{axis}
\end{tikzpicture}}

\tabfig{fig:detail_c_rac}{%
\texttt{RaC} on \texttt{mixed-forest} instances, $n\ge10\,000$.}{%
\tab{rep_detail_campaign_c_rac}}{%
\begin{tikzpicture}
\begin{axis}[sizeaxis, ylabel={\texttt{RaC} time (ms)}, legend columns=2]
\addplot[rac] table[x=n, y=independentpositive] {\dfile{rep_plotdetail_campaign_c_rac}};
\addlegendentry{ind-pos}
\addplot[rac, dashed, mark=o] table[x=n, y=correlated] {\dfile{rep_plotdetail_campaign_c_rac}};
\addlegendentry{corr}
\addplot[aux] table[x=n, y=nearties] {\dfile{rep_plotdetail_campaign_c_rac}};
\addlegendentry{near-ties}
\addplot[aux2] table[x=n, y=exactties] {\dfile{rep_plotdetail_campaign_c_rac}};
\addlegendentry{exact-ties}
\end{axis}
\end{tikzpicture}}

\section{Reproducing this report}

\begin{verbatim}
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build -j
ctest --test-dir build --output-on-failure   # correctness gate
tools/run_night_sweep.sh                     # all campaigns, one command
tools/build_report.sh                        # aggregate + emit all tables
cd report && latexmk -pdf computational_report.tex
\end{verbatim}

\noindent
Code, generators, raw and processed data:
\sloppy\url{https://github.com/fabiofurini/parametric-closure-forests}, release
\texttt{v0.3.0} (instance archives split under GitHub's asset size limit, with
SHA-256 checksums). The pseudoflow baseline is the unmodified upstream
implementation at
\url{https://github.com/hochbaumGroup/Bounded-precision-simple-parametric.git}.

\end{document}
